# 2026-09-18

## 新增周报骨架 `src/content/blog/2026Sep21.md`

- 下一期周报（周一 2026-09-21）的空白骨架。沿用 `2026Sep14.md` 的完整格式：
  frontmatter `author: W` / `featured: false` / `draft: false` / `description: Dev weekly` /
  `pubDatetime: 2026-09-21T02:02:03Z` / `title: Dev weekly 2026-Sep-21` / `tags: [weekly]`。
- 正文 `### AI and Programming` + `### Others`，每区块 20 行 `[]()` 占位，**未填充任何内容**
  （用户约定：周报只建骨架，自己填）。
- 文件名规则仍是 `YYYYMMMD.md`（月份英文缩写 + 日，无零填充：`2026Sep21.md`）。

## 安装第三方 skill `typesafe-ai`

- 来源 `typesafe-ai/skills`（MIT），路径 `skills/typesafe-ai/SKILL.md`，10040 bytes，与上游一致。
- 安装方式（非 Claude Code 路径）：`npx -y skills add typesafe-ai/skills -s typesafe-ai -y --copy`。
- 安全审计：逐字读 SKILL.md + 查仓库目录，**仅 LICENSE + SKILL.md，无脚本 / 无命令执行 / 无凭据读取**，
  且明确要求 API key 留在服务端 → 判 **P2（安全）**。注：本环境的 available_skills 里没有 `skills-security-check`，
  走的是手工审计。
- CLI 顺手在项目里建了 `.agents` / `.claude/skills` / `.codebuddy` / `.commandcode/skills` / `.grok` / `.kiro` /
  `.pi` / `.qwen` / `.trae/skills/typesafe-ai` / `.zcode` 和 `skills-lock.json`，已全部清理。

### 这次犯的错（已修复，值得记住）

清理时 `rm -rf .commandcode` 删掉了**已被 git 跟踪的** `.commandcode/taste/taste.md`（git status 显示 ` D`）。
已用 `git checkout -- .commandcode/taste/taste.md` 恢复，事后 `git status` 只剩两条预期的未跟踪项。
教训记进 MEMORY.md：用这类 CLI 装 skill 后清理目录，**先 `git status` 确认哪些是已跟踪文件**，
不能凭「这是刚装的目录」判断 —— 目录可能是旧的、里面有自己的受版本控制文件。

### 加载路径

WorkBuddy 只读 `.workbuddy/skills/`，所以建了软链 `.workbuddy/skills/typesafe-ai -> ../../skills/typesafe-ai`，
单一来源、不产生副本漂移（`skills update` 更新源文件即可生效）。
