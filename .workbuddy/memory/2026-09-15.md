# 2026-09-15

## 第十二轮：新增 skill `evidence-and-claim-review`

用户直接给了一份 32 节的完整方法论草稿（「下面这版可以直接放进你的 skills/ 目录」），
要求建成新 skill。做法是**忠实落地**而非改写：只加 frontmatter、删掉给用户看的开场说明段，
其余 32 节逐节保留（含 S0–S5 来源分级、U0–U4 用例分级、Level 0–5 落地程度、七类 theory-practice gap、
F/S/R/T 四维评分、Claim Matrix 十二列、29 节的搜索词模板、32 节的核心原则）。

- 位置：`skills/evidence-and-claim-review/SKILL.md`，1558 行
- frontmatter 按仓库约定：`name` / `version` / `description`（description 写成触发描述，含中英触发词）
- 校验：32 节编号连续、表格列数 0 异常、围栏配平、无空标题、关键术语（S0–S5 / U0–U4 / Level 0–5 / 七类 gap / F·S·R·T）齐全
- 安全自查：纯方法论文本，无脚本、无凭证、无破坏性指令；唯一的行为性内容是 §29 的搜索词模板（大量只读检索），属 P2

### 与已有 7 个评审 skill 的关系

这条 skill 是**横切件**：不替代任何一条评审流程，而是在写报告 / 出结论之前，负责判断
「这句话到底能不能这么说」。典型接点：

```text
enterprise-agent-architecture-review / 各领域深审 skill   → 产出观点与结论
        ↓
evidence-and-claim-review                                → 逐条 Claim 判定可信度与改写建议
        ↓
agent-well-architected-assessment                        → 把改写后的结论落成评分与整改
```

区别要点：assessment 审的是**控制有没有做到**（面向内部系统与证据），
claim review 审的是**说法成不成立**（面向文章、厂商宣传、benchmark、行业判断）。

## 第十三轮：用新 skill 审核 `temp/Snowflake Agent体系-成稿.md`

按 skill §28 的 14 步 workflow 走了一遍，做了 12 次外部检索做反例与时效核查，
产物落盘 `temp/Snowflake Agent体系-成稿.review.md`（按 §31 模板：Overall Verdict / P0·P1·P2 /
Claim Matrix C1–C28 / Counter Evidence / Real-world Evidence / Theory-Practice Gaps /
Rewrite / Remove / Missing Evidence / F·S·R·T Final Assessment）。

**结论**：Overall **Medium**，且必须分层说 —— Capability/FACT 层 **High**，Maturity/生产层 **Low–Medium**，
Prediction/战略层 **Low**。发布建议：修掉 P0 后可发，同时补 P1 的限定语。

### 审出来的硬问题（这条最值得复用）

- **P0-1 事实错误**：原文写 Cortex Search 单服务上限 **400M rows**，官方文档是 **<100 million**
  （超出直接创建失败）。**4 倍偏差**，且引用的 [10] 自己写的就是 100M —— 典型的「引用没被读」。
  同类漏洞还有一个：**索引时只有前 512 token 参与向量嵌入**（长文档必须预分块），原文完全没提。
- **P0-2 证据结构**：47 条引用 = Snowflake 官方 30 + Reddit 10 + OpenAI/Anthropic 5 + LangChain 2，
  **独立第三方为 0**（无 benchmark、无分析师、无 cross-vendor 研究）。
  落地证据最高 Level 2–3（POC/试点），无 Level 4–5 公开可核验案例。
  这类缺口不是「某句话错」，而是「整类结论缺一层证据」，必须单列成 P0 而不是散在各条里。
- **P1 疑似错**：§6 写 orchestration 模型「含 Anthropic、OpenAI、部分 Google 模型」，
  官方 Agents 模型清单只有 Anthropic + OpenAI，**无 Google**。
- **P1 选择性取证**：§14 引 Anthropic 自主性研究只取趋势最陡的一段，
  丢了 99.9 percentile 前提、丢了「中位数 45 秒」、也丢了源文自己写的「mid-January 之后已回落」。
  → 教训：**引用厂商研究时，必须回源读它的限定语与后续更新**，不能只取被转述的那句。
- **P1 漏限制**：OpenAI Agents API 是 **public beta**（原文写成「发布」），
  且官方明确 **data residency 仅美国、不支持 ZDR（自建 sandbox 也不因此获得）**——
  文章读者是金融机构，这条漏了是实质缺失。
- **P1 伪多来源**：[13] 与 [14] 是同一篇文档（`.com` 与 `.cn` 两个域名同路径），
  却算两条引用，且 [14] 被标成独立的「限制」页。
- **P1 链接损坏**：[4] 是 `docs.snowflake.com/en/en/release-notes/...`（双 `/en/`）；
  [36] 带垃圾 query 参数（`?-what-the-heck-is-rust%2F=undefined&hubs_...`）；
  全 47 条 URL 带 `utm_source=chatgpt.com`（另有 `_bhlid` / `trk` / hubspot 参数）。
- **P1 措辞越界**：§8 把 10 条 Reddit 写成「实际使用者反馈呈现三类**稳定共识**」
  （社区证明 experience，不证明 prevalence）；§9 把带数小时滞后的 budget 动作写成
  「已具备平台级 enforcement，不止 reporting」。
- **P1 把已知限制写成观点分歧**：§2 Autopilot 写成「有人认为 80–90%，也有人认为…」，
  实际有可核验的硬限制（LOD / 混合数据源不支持迁移、文件 <250MB、依赖 query history 质量）
  与独立实测反证（生成的 metric 未做快照去重，最多放大 14 倍）。
  → 教训：**「有人说…也有人说…」是最容易掩盖已知事实的句式**，审核时要把它翻成限制清单。

### 值得肯定的两处（改写建议里应保留）

- §7 主动列出四条官方 Evaluation 限制（MCP 不进 evaluation / replay 不传 session attributes /
  code execution 无 side effect / evaluation 有成本），并把 Assurance 拆成八项区分
  「Evaluation ≠ Authorization Test ≠ Security Test ≠ Regulatory Evidence」。这是全文最有价值的一段。
- 主动**没有**引用 Snowflake 自家的 accuracy 数字（>90% 出自 2024-08 内部 150 题集，此后未更新）
  与 47%→83% 的内部 benchmark —— 属正确规避厂商 benchmark 陷阱。

### 方法论上的收获（可写回 skill）

1. **P0 有两类**：一类是「某句话与来源不符」（如 400M），一类是「整类结论缺一层证据」（独立证据为 0）。
   两者都要进 P0，但处置方式不同（前者改字，后者改结论强度或改标题定位）。
2. **数字类 Claim 必须回源核**，尤其限额、QPS、行数、版本日期 —— 这类最容易被转述成错值，
   而且错值会被下游拿去做容量规划。
3. **厂商研究类引用要核对三件事**：被引用的统计量是否带前提（percentile / 样本 / 时间窗）、
   源文是否有后续更新或回落、源文自身的限定语是否被保留。
4. **把「观点分歧」句式当成红旗**：见到「有人认为…也有人认为…」先查是不是其实有可核验的已知限制。

## 第十四轮：按用户 17 条 review 重写 `evidence-and-claim-review` → v2.0.0

用户给了一份很完整的 review，核心批评是两条：**概念混在一起**（来源等级把「类型/权威/独立性」塞进一根轴）、
**写作太像方法论文章、不够 Agent 可执行**。要求「严格按照审核建议修改」。

做法不是打补丁，是**按四层模型重构**：

```text
1. Claim Model      抽取 / 类型（+LIMITATION）/ Fact·Interpretation·Prediction / Scope / Claim Context
2. Evidence Model   五轴证据质量 / Source Type T0–T5 / Independence IND0–IND5 / Source Correctness /
                    Freshness / 反例与 Silent Evidence / Benchmark Validity / 厂商四条硬规则
3. Reality Model    Use Case Status（Exists→Outcome-verified）/ Production Level L0–L6 /
                    案例证明了什么 / 案例客观性 / 七类 gap
4. Review Workflow  17 步 Required Actions / Stop·Continue Conditions / F·S·I·R·T /
                    Status × Confidence / 改写动作 / 输出模板 / YAML Claim Record
+ 附录 A 检索词模板、附录 B 术语与编号（含 v1 → v2 对照表）
```

结果：1647 行 / 26KB → **960 行 / 38KB**。行数减了，字节涨了 —— 删掉的是重复讲法和小节碎片，
加上的是 7 个新维度。对外要说清楚这是**结构变简、内容变厚**，不是纯压缩。

### 三处最关键的修正（这三处决定了 skill 是否可用）

1. **`S0–S5` 单轴拆成 `Source Type T0–T5` + `Authority` + `Independence IND0–IND5`**。
   旧 S0 里「官方文档 / 论文 / RFC / 源码」并列，但它们的权威性完全不同。
   新规则：T0 的 Authority=5 **只在回答主体自己的事实时成立**；一旦 Claim 变成 comparing/maturity，
   T0 立刻退化为 interested party，Authority 降为 1。这一条解释了为什么「引用全是官方文档」
   撑不起一篇成熟度评估。
2. **Confidence 不由独立性决定**。旧规则 `High = 一手 + 最新 + 直接 + 独立 corroboration`
   会把所有能力事实系统性误判成 Medium（因为「产品支持 X」本来就只有厂商一个权威源）。
   新规则按 Claim 类型分流：FACT/CAPABILITY/LIMITATION 型允许 `Confidence HIGH + Independence LOW`；
   PERFORMANCE/MATURITY/COMPARISON/PREDICTION 型不得只凭厂商来源给 High。
3. **`I — Independence` 升为正式评分维度**（旧的四维 F/S/R/T → 五维 F/S/I/R/T）。

### 新增的维度里，价值最高的两个

- **`Scope / Applicability`**：判据是 `证据 scope ⊇ claim scope`。
  这是「证据被错误泛化」这类错误唯一的检出手段 —— 所有证据都真，结论仍可错。
  与上一轮审 Snowflake 成稿时的发现直接对应（「Cortex Search 已成熟」实际只覆盖
  Snowflake-native + medium scale）。
- **`Silent Evidence` + `Absence of counter-evidence ≠ Evidence of absence`**：
  把「搜了没找到」和「没搜」分成两个结论。要求记录 `searched / found / plausible_reasons`，
  并列出四条结构性原因（内部事故不公开 / 失败 POC 不公开 / case study 挑成功 / 社区 selection bias）。

### 可执行化的两个构件

- **§4.1 Required Actions**：17 步，每步留可核对记录。
- **§4.2 Stop / Continue Conditions**：明确何时必须继续搜（类型是 MATURITY/COMPARISON 却只有单一来源、
  涉及数值、涉及 GA·Preview 状态、用 benchmark 做比较）、何时可以收工
  （FACT/CAPABILITY + 当前官方一手来源 + 版本日期已确认 + 无显式反证）、
  何时**必须降级结论**（只有厂商来源却判 maturity → 最高 CONDITIONALLY_SUPPORTED）。
  这一节是 v1 → v2 性质变化最大的地方：v1 讲「要判断权威性」，v2 讲「满足什么条件才能收工」。

### 校验方式（可复用）

脚本检查：控制字符为零、围栏按长度配对平衡、表格管道符计数分组自洽（3/4/6/14）、
重复实义行扫描（9 处全部是 YAML 字段回显，非冗余散文）、逐项确认 17 条 review 落地、
残余 `S0–S5` 只出现在附录 B.3 对照表。

**保留的一条历史纪律**：v1 第 32 节的核心原则（不要只找支持证据）没有被削弱，
而是升级成了 15 步主线链：
`Claim → Type → Scope → Evidence → Directness → Authority → Independence → Freshness →
Counter-evidence → Use Case → Outcome → Reality Gap → Status → Confidence → Rewrite`。

---

## 第十五轮：按 v2 口径重跑 Snowflake 成稿审核

用户指令「重新按照v2跑一遍」。`temp/Snowflake Agent体系-成稿.review.md` 被**整体覆盖**为 v2 口径
（667 行 / 50KB / 13 个 H2 节），早期 v1 版不再存在 —— 以后引用该文件默认指 v2 版。

### 为什么值得重跑（不是换词重述）

v2 加的三个维度真的改变了结论，不是换个说法：

1. **`Use Case Status`（Exists→Adopted→Successful→Scaled→Outcome-verified）**
   v1 只判到「有落地案例」，用 Level 0–5 给了个偏高的印象。v2 逐案拆开后落到 **Adopted ≈ L4**，
   并显式补上一句 **`L4 ≠ Successful ≠ Enterprise-grade`** —— 这是 v1 结论里最需要修正的一处。
2. **`Independence` 与 `Authority` 拆开**后，两个 P0 才显形：
   - **Cortex Search 授权归属写反**（P0）：官方设计是 **owner's rights** 运行 —— USAGE 只校验 owner 的
     读权限，caller 的表权限、masking、RLS 一律不参与。这既是设计行为、也是默认配置下的**数据泄露路径**，
     要写成「设计行为 + 3 条架构缓解」，不是原文的「取决于建模对齐」。
   - **400M → <100M**（P0）：Cortex Search 单服务基表行数上限。
3. **Silent Evidence 让「没有反证」不再等于「证据充分」**：47 条引用全为厂商文档 + Reddit，
   零独立 benchmark / 分析师 / cross-vendor 研究 —— 这一条在 v1 里只是「来源偏单一」，
   在 v2 里升级为需记录 `searched / found / plausible_reasons` 的结构性缺口。

### 新出现的两条 P1（v1 没有）

- **C25 战略性排他论点 CONTESTED**：原文称前沿厂商「无法提供 Identity / Entitlement / Policy」，
  但 Snowflake Summit 26 已自定位为 LLM 与企业系统之间的 **control plane**，并给出 Horizon Context
  （业务术语表 / 血缘 / 访问策略 / agent 权限聚合）与 Cortex Agents 调用携带 **Agent Identity**、由
  Trust Center 审计 —— 时间早于该文，论点被自家厂商路线图反证。
- **Gartner Peer Insights（2026-04）作为独立反证**：「无原生健壮的 LLM 评估框架」、可观测/调试缺口、
  细粒度成本归因困难、厂商锁定担忧。这是全文极少数非厂商来源之一，方向为负。

### 结论分层（v2 的核心表达方式）

不再给单一总分，而是按 Claim 类型分层：**Fact/Capability 高 · Maturity 低–中 · Comparison 低 ·
Prediction/Strategy 低–中**。`Status × Confidence` 两组正交 —— 全文多数条目落在
`SUPPORTED + Medium` 与 `CONDITIONALLY_SUPPORTED + Medium`，战略类落在 `CONTESTED + Low`。

### 与 v1 review 的差异（写进 Final Assessment 对照表）

| 处 | v1 | v2 |
|---|---|---|
| 落地等级 | 「最高 Level 2–3」 | Adopted ≈ L4，但 L4 ≠ Enterprise-grade |
| Cortex Search 授权 | 「取决于建模对齐」（错） | owner's rights 设计行为 + 3 条缓解（REVERSE） |
| 排他性战略论点 | 未质疑 | CONTESTED |
| 结论形态 | 单点总分 | 按 Claim 类型分层 |

### 工艺

覆盖写入，未保留 v1 副本；结构与 13 个 H2 节沿用 §31 模板。校验：控制字符为零、
围栏平衡、Claim Matrix 28 行每行 14 个管道符、13 个 H2 节齐备、50,572 字节。
`MEMORY.md` 同轮做了瘦身（12.4KB → 11.2KB），去掉 skill 树与 checklist 条目的重复叙述，
并把「review 是 v1 口径」的过期说明改为「现为 v2 口径，已覆盖 v1」。

---

## 第十六轮：`evidence-and-claim-review` v2.0.0 → v2.1.0（模型正交化，随后冻结）

用户就 v2 审核的实际运行结果提出第二轮 review，核心判断是：**v2 的方法论已成立，不需要推倒重写；
但内部模型还有「把两个不同维度压成一个字段」的问题**。定为 v2.1 精修，共 7 条（P0×3 / P1×3 / P2×1），
外加 review 正文里散落的 4 条。用户明确要求：**「v2.1 应该做模型正交化 + negative claim + evidence sufficiency
+ applicability + outcome verification，然后冻结。」**

### 最重要的一处：Claim Type 与 Epistemic Status 正交化

原 v2 的 §1.3 是「Fact / Interpretation / Prediction 三选一且互斥」，但同时 §1.2 又有一张 13 类的
Claim Type 表 —— 两套分类混用。用户给的例子说明问题：

```text
「Cortex Agent 是 Data-Native Managed Agent Runtime」
  Claim Type = STRATEGIC      Epistemic = INTERPRETATION     ← 两个字段，不是一个
「Cortex Agent Evaluations 已于 2026-03-13 GA」
  Claim Type = FACT           Epistemic = FACT
「Frontier Agent Runtime primitives 正在快速商品化」
  Claim Type = PREDICTION     Epistemic = INFERENCE
```

改为两个正交字段：`type`（问哪类问题）× `epistemic`（这句话本身是事实/推论/预测/意见）。
分工写清楚了：**Type 决定要多少证据，Epistemic 决定这句话的强度上限。**
`epistemic = PREDICTION` 一律不得判 SUPPORTED；`epistemic = OPINION` 不得作论证前提。

### 其余六条

- **Evidence Sufficiency（新 §2.9）**：`SUFFICIENT / PARTIALLY_SUFFICIENT / INSUFFICIENT`。
  与 Confidence 的分工是：Confidence 答「我有多确定」，Sufficiency 答「手头的证据够不够回答这类问题」。
  价值在于抓「看起来很有把握、其实答非所问」—— 例如问成熟度却只给能力证据。
  现在共**三个正交结论**：Status × Confidence × Sufficiency。
- **Negative claim 规则（§1.2 新增）**：LIMITATION 的取证必须满足三条之一（官方明写 unsupported /
  schema·API·runtime 明确排除 / 可重复实验确认）；只凭「文档没写」最高 INSUFFICIENT_EVIDENCE。
  写成一句判据：`absence in docs ≠ explicitly unsupported`。
  并说明了它与「缺席的反证 ≠ 反证不存在」是同一纪律的两面。
- **Applicability Conditions（新 §1.5）**：Scope 回答「到哪里为止」，Conditions 回答「拿什么换」。
  条件是 `required / required_for_write / recommended`，**含未满足的 required 则最高 CONDITIONALLY_SUPPORTED**，
  且必须是可核对对象（角色/流程/配置/数据质量），不能写成「取决于治理成熟度」。
- **Outcome Verification（新 §3.2）**：`Successful` 从来没说清「是谁定义的」。
  拆为 `NONE / SELF_REPORTED / REPRODUCIBLY_REPORTED / INDEPENDENTLY_VERIFIED`。
  关键判据：独立验证的核心是**验证方与被验证方无利益关系**，不是「写得详细」。
  `SELF_REPORTED` 的数字不得写成既成事实，必须保留归因。
- **Counter Evidence Severity（§2.6 新增）**：`Cosmetic / Operational / Economic / Reliability /
  Governance / Safety-Security`。规则是**严重度不可平均** —— 一条已确认的 Governance 级反证权重高于
  任意数量 Cosmetic 反证之和。本轮 Snowflake 案例里 Search owner's rights 正属此列。
- **搜索深度按 Claim Type 分级（§2.6 表 + §4.1 三级）**：取消了「每 Claim 至少 4–6 个反证变体」的一刀切。
  反证搜索方向也按类型走 —— 例如 LIMITATION 的反证搜索是**反向的**（去找「其实支持」的证据）。
  §4.1 的 17 步改为三级：Level 1 Basic Fact（6 步）/ Level 2 Decision Claim（+8 步）/
  Level 3 Strategic Claim（+4 步）。理由写进 SKILL：战略预测类条目往往只占少数，
  但应消耗与其余全部条目相当的时间，混在一起逐条跑完整流程是最大的效率浪费。

### review 正文里额外落地的四条

- **「无反证」从 SUPPORTED 的积极条件改为独立轴 `counter_evidence.strength`**（none_found / weak / strong）。
  用户指出的矛盾很准确：既然写了「缺席的反证 ≠ 反证不存在」，就不该把「无反证」写进 SUPPORTED 定义，
  否则 Agent 会对一条官方明写的能力事实也做完反证搜索才敢判 SUPPORTED。
- **Authority 概念化**为 `Expertise × Proximity × Claim-Fit`（再按 conflict of interest 扣减），
  原来的 5×4 对照表降级为「模型的结果，供快速参考」。
- **Search Coverage（新 §2.7）**：五种偏差（selection / ranking / survivorship / language / geographic），
  用途是给 `independent_sources = 0` 提供解释依据 —— 必须能区分「没有证据」与「我们没搜到」。
- **五条不可违反原则**（原四条）：新增第五条 —— 证据缺席只降低 Evidence Confidence，
  **不自动推出产品负面结论**。`independent_sources: 0` 是关于证据结构的结论，不是关于产品的结论。
  给出了正误写法对照。

### 校验

17 项检查全过（7 条 requested + 4 条附加 + 6 项「勿回退」的 v2 存量）。结构校验：控制字符 0、
围栏按长度配对平衡（` ``` `×156 / ` ```` `×2）、表格管道符分布 {3:101, 4:77, 6:15, 17:1}、
重复实义行 4 处均为有意复用（同一示例在原则与细则各出现一次）。
体量 959 行 / 38KB → **1472 行 / 62KB**。

### 冻结约定

写进 **附录 B.7**：后续若发现新问题，优先改进**判据清晰度**（一个字段的边界、一个规则的例外），
不再增加**新的维度或新的来源类型** —— 维度已足够多，再加只会让 Agent 执行时更容易漏项。
B.7 同时列出了 v2.1 相对 v2.0 的 11 项变更，供未来维护对照。

### 本次遗留

`temp/Snowflake Agent体系-成稿.review.md` 仍是 **v2.0 口径**，未经 v2.1 重跑 ——
它还没有 Epistemic / Sufficiency / Severity / Applicability / Outcome Verification 字段。
若要验证 v2.1 的实际效果，需要再跑一轮。
