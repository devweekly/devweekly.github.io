# 2026-08-19 工作日志

## Cumora 架构与 Agent 设计文档（ref-only/cumora）
- 分析了 `ref-only/cumora`（yetone/cumora v0.1.64）的架构与多 Agent 设计，产出文档 `ref-only/cumora/ARCHITECTURE_AND_AGENT_DESIGN.md`。
- 本轮以代码级证据补全四大块（§9–§12）：
  1. **支持范围**：确认"用户可接入的本地 BYOA 客户端仅 Claude Code / Codex"——`engine.ts:87/90` EngineId、`:1407` ADAPTERS、`registry.ts:48` PAIRABLE_ENGINES、`agent-cli/src/cli.ts:24` 与 `daemon.ts:451` 强制文案、`daemon.ts:605` --engine 校验。云端 `managed`（OpenAI Responses API）是独立第三种引擎，非用户客户端。架构可扩展（新增 EngineAdapter 即可）。
  2. **通讯架构**：双平面——控制平面（SSE `/runtime/wake-stream` + `POST /runtime/cli` 代执行，wake-bus.ts 证据）+ 数据平面（`cumora` 子命令作用于共享 DB，Agent 间无直连）。
  3. **角色定义**：澄清 `role` 仅是 `participants` 自由文本列，**无硬编码角色原型**；主/研究/测试 Agent 是配置示例，差异来自 system_prompt/memory/skills。人类与 Agent 完全同级。
  4. **协作流程**：去中心化 + 共享介质 + 服务端闸门 + GLANCE_YIELD_RULES 宪法，涌现性协作；附落地页任务拆解示例与 chain/counting 基准实证。
- 关键源码位置：engine.ts、registry.ts、wake-bus.ts、daemon.ts、personas.ts（GLOBAL_RULES）、runtime/orchestrator.ts、runtime/pod-agent.ts。

## 追加：Prompt 设计分析（§13）
- 新增 §13 "Prompt 设计：值得学习的工程实践"，12 条可迁移经验，源码证据：
  - standing/delta 分层（daemon.ts:1316-1390，曾膨胀被回滚极简）；
  - GLANCE_YIELD_RULES / AGENT_VOICE_RULES 单一事实源 verbatim import（glance-protocol.ts:4-6, agent-voice.ts:4-8）；
  - triage 小模型纯闸门只判 actionable、不 route（triage-core.ts:25-43, 176-195）；
  - AI-native：分类用模型、正则只解析模型输出（triage-core.ts:10-17, 123, 149）；
  - failClosed 方向感知（纯 Agent 环路关、有人类开，:80-85, 492-495）；
  - 确定性地板 HARD_LOOP_CAP + 自缩放 lap floor（:207-209, 472-478）；
  - 强人设 frame + 长活先 ack（agent-voice.ts:13-29）；
  - 每回合注入 UTC 时钟（daemon.ts:1379，werewolf 惨案）；
  - 发前 glance 软提示 + 服务端 HOLD 硬兜底；名册运行时注入；CLI 特殊字符写成 prompt 规则（:1339-1345）。
- 核心结论：prompt 哲学与架构同源——代码机制优先于 prompt，prompt 只承载 shape 级原则，且极简/分层/单一事实源/配确定性地板。
