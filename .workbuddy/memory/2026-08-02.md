# 2026-08-02

## repo-arch-engineering skill：报告结构精简 + Hard Gate 下调

用户要求从 SKILL 与所有 sub-agent 中移除报告 §7 Quality Attributes / §8 Risks and Debt / §9 Unknowns / Appendix A / Appendix B。

### 改动清单
- **SKILL.md**：§6.4 报告结构块移除上述 5 段；§6.7 Hard Gate 阈值 15000 → 12000（用户选定）；§6.7.1 路由保留。
- **agents/report.md**：同步移除 5 段，§10 → §7 重编号；阈值 15000→12000。
- **agents/quality.md**：完整性检查改为「§1-§6，无 QA/Risks/Unknowns/Appendix」；gated-fail 示例阈值同步 12000。
- **agents/planner.md / reasoning.md / model.md**：移除 Unknowns / quality_attributes / risks 字段引用与「字段分区」写入说明。
- **model-schema.md**：§18 Quality Attributes/Risks/Unknowns Model 标记「已废弃」（保留说明，不删节以防断链）；去除 JSON 字段、ID 前缀 qa-/risk-/unk-、coverage 检查项。
- **DESIGN.md / Methodology.md**：目录树与叙事骨架移除 quality_attributes/risks/unknowns/Top Risks 引用。
- **scripts/check-report-chars.mjs**：`MIN_REPORT_CHARS` 默认 15000 → 12000（注释同步）。

### unsloth 报告（.working/unsloth/report.md）
- 删除 §7/§8/§9 + 两附录，§10 Blast Radius 重编号为 §7；清理正文 dangling 引用：`risks rk-*` 标签（§4 两处）、「详见 §7/§8」、§7 内 `rk-*` 引用；补 4 处真实深度（patch 机制、入口锚点、扩展成本、Studio 失败隔离）后过 Gate（12243 ≥ 12000）。
- 注意：原「extensibility 评级为 medium」引用了已删 §7，已改为自包含表述。

### 关键约定（延续）
- Evidence Level（报告侧 S/A/B/C/D/E）≠ Evidence Tier（采集侧加权），Appendix B 已随附录删除一并移除。
- 原 §5.1 Boundaries / §6.2 Backpressure 不在本次移除列表，保留；§6.2 已补「失败隔离」半段使其名副其实。

## p7.md 对照增强：报告升级为 11 章节成熟结构

参考 `p7.md` 架构报告成熟度标准，审计并增强 unsloth 报告 + 同步 SKILL/sub-agents。

### 审计报告发现的缺陷
1. **phantom evidence `ev-222`**：出现在 §4.3 dd-3 与 §6.1 step 2，但 evidence-log 仅 ev-001~ev-030 → 已改为 ev-022。
2. **dangling `c-1..c-5` 约束引用**：每个决策标注 implements c-N，但报告从未定义 → 新增 §3.2 Driving Constraints 显式定义 5 个硬约束。
3. **缺失 p7.md 强制章节**：System Context(C4 actors)、Architecture Style 显式命名、Data Architecture、Architecture Strengths、Architecture Risks(聚焦)、Engineering Insights 全部缺失。

### 报告结构（最终 11 章节）
1. Executive Summary（含 Thesis + 最大 trade-off + 4 条技术债务）
2. System Identity & Context（2.1 What / 2.2 System Context C4 / 2.3 Business / 2.4 High-Level Arch）
3. Architecture Overview（3.1 Style 显式三风格 / 3.2 Constraints / 3.3 Central Idea / 3.4 If Removed / 3.5 Timeline）
4. Key Design Decisions（7 个 dd-1~dd-7 + 决策耦合）
5. Resulting Architecture（5.1 Boundaries / 5.2 Extension）
6. Data Architecture（6.1 数据模型 / 6.2 数据流 / 6.3 约束）
7. Runtime Realization（7.1 One Request / 7.2 Studio 流 / 7.3 Backpressure & Failure Isolation）
8. Architecture Strengths（S1~S5，证据支撑）
9. Architecture Risks（R1~R3 聚焦 + R4 边界，p7.md §10 必含）
10. Change Difficulty & Blast Radius
11. Engineering Insights（6 条工程哲学，p7.md §14 最高价值）

### SKILL / sub-agent 同步
- **SKILL.md §6.4**：更新为 11 章节模板（含 System Context / Data Architecture / Strengths / Risks(聚焦) / Engineering Insights）。
- **agents/report.md**：结构块同步 11 章节；内部引用 §5/§7、§10、§2.2 System Context、§6 Data Arch、§3.1 Style 全部对齐。
- **agents/quality.md**：完整性检查改为「§1-§11，含 System Context/Data Architecture/Strengths/Risks(聚焦)/Insights；无 Appendix」；新增 §2 System Context / §6 Data Arch / §3.1 Style 显式检查项；Executive Summary 加「主要技术债务」。
- **model-schema.md §18**：精确化为「综合版字段已废弃，报告 §8/§9/§11 由 Report Agent 叙事渲染，不从模型字段生成」。

### 复检
- Hard Gate（默认 12000）：报告纯内容字符 **20213 ≥ 12000，PASS**。
- 28 个 ev- 引用全部在 evidence-log 真实存在（无 phantom）。
- 无 Quality Attributes/Risks and Debt/Unknowns/Appendix A/B 残留（仅 model-schema §18 废弃说明有意保留）。
