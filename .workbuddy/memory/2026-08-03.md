# 2026-08-03

## repo-arch-engineering skill：p8.md 叙事化改造（全量同步 SKILL + Agents）

用户参考 p8.md（报告阅读体验批判），要求"完完整整"改造 SKILL 与全部 sub-agent。p8.md 核心：报告像"架构审计数据库导出"不像"架构师写给工程师的分析文档"——缺叙事层/因果链、confidence 侵入正文、定义句过多、章节无过渡、bullet 过多、术语无解释、缺架构师总结。

## 文档混杂检查：SKILL vs DESIGN vs Methodology 分工清理

用户质疑 SKILL.md 是否又混入 DESIGN/Methodology 内容。逐段对比后确认有混杂并修复：

### 发现的问题
1. **SKILL.md L199 复述 Methodology**：「核心原则：Architecture 是 Decision 的结果」直接抄自 Methodology §叙事骨架原则 → 改为执行指引+指向（"Report 理论详见 Methodology §Report Theory"）。
2. **叙事细则双份**：p8 叙事规则同时写了 SKILL §6.4.1（7 条执行细则）与 Methodology「叙事表达原则」（6 条）→ Methodology 改为**理论层**（为什么叙事：因果链/Evidence 分离/观点段/术语/过渡/EM 的 rationale），执行细则只留 SKILL §6.4.1；三层结构同理（理论在 Methodology，执行见 SKILL §6.4.1 ①）。
3. **Evidence Level 双源**：DESIGN.md 声明报告侧 Level「定义处 = SKILL §6.5」，但 Methodology 也复制了完整表 → Methodology 删表改为指向 SKILL §6.5（权威）+ model-schema §9.1（采集侧 Tier）。
4. **SKILL L623 小写 narrative 残留**：Sub Agents 表 Quality 职责"检查 narrative 质量门"→ 改 "Insight 质量门"（上次只查了大写 Narrative，漏了小写）。

### 修复后的分工（单一权威原则）
- **SKILL.md** = 执行规范：§6.5 Evidence Level 表（报告侧权威）、§6.4.1 叙事渲染执行细则、Hard Gate 等；理论一律指向 Methodology
- **Methodology.md** = 稳定理论：叙事表达原则（为什么）、三层结构理论、Evidence Level 理论（定义指向 SKILL §6.5）；执行细则一律指向 SKILL
- **DESIGN.md** = 设计决策理由（不动）
- **agents/report.md** = 执行速查表（Level 表保留，已加"定义权威见 SKILL §6.5"）

### 校验
- narrative（大小写）全 skill 无残留 ✓
- "Code + Test + Formal Verification" 表仅 SKILL（权威）+ report.md（速查）两处 ✓
- SKILL 指向 Methodology 共 7 处 ✓；Methodology 指向 SKILL 共 3 处 ✓

## 新增 Report Editor Agent（编辑阶段，pipeline 升级）

用户提出增加 **Architecture Report Editor Agent**：Research 优化「信息完整性」，Editor 优化「人类认知负荷」——把「自动生成架构分析报告」提升到「Google/Meta 内部 Architecture Review Document」可读性。

### 新 pipeline
```
repository-model.json → architecture-insight.json → report-draft.md（Report Agent 初稿）
    → report-edited.md（Editor Agent 编辑稿）→ Quality PASS → report.md（Workspace rename）
```

### 改动清单
1. **新建 agents/editor.md**（第 12 个 agent）：编辑 ≠ 改写——不读代码/不研究/不新增 claim
   - 6 个编辑动作：信息裁剪（按认知分组）、主线提炼（从 insight.thesis 提升）、密度控制（正文/Implementation Detail 分层）、阅读导航（Before-reading + 每章 intro）、概念索引（Concept Index：primary_location 首现详解 + references）、章节比例（Story 35% / Mechanism 25% / Decision 20% / Evidence 10% / 导航 10%）
   - 3 Pass：Structural Editing → Reader Optimization → Technical Guard
   - 8 条准确性约束：不新增无 evidence claim / 不删 high-confidence 结论 / 可移 evidence 但保留 ev-xxx / 可压缩重复 / 可加解释不改事实 / 必须保持 trade-off / 不得弱化风险 / 不得篡改数值
2. **SKILL.md**：mermaid + Step 6 描述 + 新增 §6.7 Editorial Review（编辑阶段）→ 原 §6.7 Hard Gate 顺延为 §6.8、§6.7.1 → §6.8.1；Working Folder 树加 report-edited.md；Sub Agents 表加 Editor 行（Report→Editor→Quality 内部顺序）；成功标准补 Editor 维度
3. **agents/orchestrator.md**：调度链 Report→Editor→Quality；mermaid 加 EDI；回炉路径三步（重渲染→重编辑→重跑 gate）
4. **agents/quality.md**：检查目标 report-draft.md → report-edited.md（对照 draft 校验编辑是否破坏准确性）；gate 脚本路径改 report-edited.md；新增 §12 Editor 编辑质量检查（9 项：Before-reading/章节 intro/Concept Index/无新 claim/高置信结论保留/trade-off 保留/风险未弱化/数值未篡改）；suggestion 新增 7 条（editor-before-reading-missing / editor-chapter-intro-missing / editor-concept-repeat / editor-new-claim / editor-tradeoff-lost / editor-risk-weakened / editor-evidence-tampered）
5. **agents/workspace.md**：发布 rename report-edited.md → report.md；崩溃恢复检查 edited
6. **agents/report.md**：职责明确「初稿优先完整性+技术密度，可读性编辑归 Editor」；§6.7→§6.8 引用同步
7. **Methodology.md**：Report Theory 新增「Editorial Review（编辑理论）」（为什么独立编辑阶段/编辑≠改写/准确性约束理论基础/Concept Index 范式）；agents 数量 11→12
8. **model-schema.md §2.1**：布局表加 report-draft.md（Report Agent）+ report-edited.md → report.md（Editor/Workspace）
9. **CHANGE_LOG.md / agents/resume.md / agents/planner.md**：§6.7→§6.8 引用、rename 路径、崩溃恢复检查点同步

### 校验
- §6.7 残留无（Editorial 正常引用除外）；§6.8 引用分布 8 文件 ✓
- agents 目录 12 个文件；Sub Agents 表 12 行 agent ✓
- gate 脚本对任意文件名可用（report-edited.md 检查点验证通过）✓

### 约定（延续）
- 产物链：report-draft.md（初稿）→ report-edited.md（编辑稿）→ report.md（发布）
- Editor 是纯表达层优化，Technical Guard 保证不破坏研究准确性；编辑稿可被 Quality 对照 draft+model 复核

### 核心变更
1. **产物改名**：`architecture-narrative.json` → `architecture-insight.json`（schema v2.0）
   - 六要素洞察结构：`intent / mechanism / constraint / tradeoff{gain,cost} / evidence / engineering_meaning`
   - `thesis` 新增 `why_this_center`（因果链，非定义句复述）
   - `boundaries[]` 升级为对象数组（why_exists / mechanism / tradeoff / engineering_meaning）
   - `runtime_story.one_request` 升级为 `{story, engineering_meaning}`
   - 新增 `architecture_strengths[]`（基于证据 + engineering_meaning）
2. **SKILL.md §6.4.1 新增叙事渲染规则（7 条强制）**：
   - ① 三层结构（Story→Detail→Evidence）
   - ② Evidence Box（evidence 移出正文，节末引用块）
   - ③ 因果链叙事（先"为什么"后"是什么"）
   - ④ 章节过渡句
   - ⑤ 观点段先于清单
   - ⑥ 术语人话解释层（"简单来说"）
   - ⑦ 每节结尾 Engineering Meaning
   - ⚠️ Neutrality 与叙事不冲突（叙事是表达层，禁夹带绝对化结论）
3. **SKILL.md §6.5**：claim 标注格式从内嵌式改为 Evidence Box 展示（正文禁内嵌 `（confidence: X · evidence_level: Y · evidence: ev-xxx）`）
4. **SKILL.md §6.6 禁止项**：新增 8 条（内嵌 evidence / 定义句开篇 / 无过渡 / checklist / 术语不解释 / 缺 Engineering Meaning / 叙事夹带绝对化）
5. **§6.4 决策展开**：Context/Alternatives/Trade-off/Implements Constraint → 六要素（Intent/Mechanism/Constraint/Trade-off/Evidence/Engineering Meaning）
6. **agents/report.md**：schema v2.0 同步 + 渲染规则重写 + 禁止项同步
7. **agents/quality.md**：§8 改 Insight 质量门（六要素检查）、§9 改 Evidence Box 检查、新增 §9.1 叙事规则检查（7 项）、suggestion 新增 10 条（evidence-inline / story-causal-chain-missing / transition-missing / term-explanation-missing / engineering-meaning-missing / checklist-style / insight-six-elements-missing 等）
8. **Methodology.md Report Theory**：新增「叙事表达原则（Report as Story）」6 条 + 三层结构；Evidence Level 补 Evidence Box 展示说明
9. **DESIGN.md / model-schema.md**：产物名与压缩层描述同步

### 保留不动
- 模型字段层 `design_decisions[]`（Context/Alternative/Trade-off）——采集/推理侧原始产物，与报告侧 insight 六要素（渲染层）不冲突
- CHANGE_LOG.md（context.json 状态规则，与报告无关）
- llm-runner.mjs / scripts/（无相关引用）
- Hard Gate 脚本与 12000 阈值不变

### 校验
- 全 skill 无 `Narrative`/`architecture-narrative` 残留 ✓
- `architecture-insight` / `Insight 质量门` / `insight-missing` 等新术语全文件一致 ✓
- gate 脚本正常（默认 12000）✓

### 约定（延续）
- Pipeline：repository-model.json → architecture-insight.json → report.md（三层，任何一层不可跳过）
- Evidence Box 是唯一允许的证据展示方式；机器可消费性由 Box 内 ev-xxx 引用与 confidence 数值保留保证

---

## 17:50 用最终 pipeline 重跑 unsloth（rerun）

用户指令「重跑一遍 unsloth」。上游产物（repository-model.json / evidence-log.jsonl / hypotheses.json / context.json）已全部 converged（coverage 全 1.0），直接复用，只重跑**报告生成管线**：

1. **重建 architecture-insight.json (v2.0)**：旧 `architecture-narrative.json` 是 v1.0，缺六要素与 `thesis.why_this_center` 因果链 → 据 model 重建 v2.0（key_design_decisions 压缩为 5 个六要素 + engineering_meaning）。旧 v1 文件已删除。
2. **report-draft.md（Report Agent）**：把旧 report.md 的**正文内嵌证据标注**全部迁移到章节结尾 **Evidence Box**（28 个）；补因果链开头、三层结构、Engineering Meaning。
3. **report-edited.md（Editor Agent）**：叠加 Before reading 总纲 + §2–§11 各章「本章回答」intro + 末尾 Concept Index；未改事实/证据/权衡。
4. **Hard Gate PASS**：report-edited.md 纯内容 20706 ≥ 12000（exit 0）；正文内嵌证据标注 0 处。
5. **发布**：cp report-edited.md → report.md（38950 字节，536 行）；context.json `analysis_target_commit` 置 null。

产物链（现 on disk）：architecture-insight.json → report-draft.md → report-edited.md → report.md。
验证：新管线实际可产出合规报告，Editor 层未破坏准确性。

---

## 18:07 报告产物重复问题排查 + 方案 B 修复

用户发现 `.working/unsloth/` 下 `report-edited.md` 与 `report.md` 内容一模一样，怀疑 SKILL/sub-agent 出错。

### 排查结论
- 三段产物链（draft → edited → report）是 `repo-arch-engineering` skill 的**设计预期**；edited→report 发布保留内容，故两者内容一致**不是内容 bug**。
- 真正问题：skill 内部自相矛盾 + 上次发布用了 `cp`（非 skill 写的 `rename`）→ 两份文件并存。
- **根因**（见本日志 17:50 第 110 行）：上次发布执行的是 `cp report-edited.md → report.md`，而 SKILL 规定是 `rename`，导致 report-edited.md 残留。

### 方案 B（干净 rename）修复清单
1. **SKILL.md 产物目录树（L617-619）**：删去常驻的 `report-edited.md` 行，改为仅在 report.md 注释说明「Editor 生成 edited 经 Quality 后由 Workspace rename 发布，发布后 edited 被消费，仅留 report.md」。
2. **agents/resume.md（L43-45）**：发布判定改为「report.md 存在 → done（即使 edited 残留也以 report.md 为准）」优先，再判断 edited 存在但未发布 → workspace 崩溃恢复。避免残留 edited 被误判为未完成。
3. **agents/workspace.md 发布步骤**：rename 处加 ⚠️ 注释「文件系统 rename/mv，源被消费，禁止 cp 复制，否则残留重复文件」，钉死语义防止下次再被实现成 cp。
4. **清理本次运行**：`rm .working/unsloth/report-edited.md`（内容已 1:1 在 report.md）。最终磁盘仅 `report-draft.md` + `report.md`。

### 改动文件
- `/Users/saga/code-repos/devweekly.github.io/.trae/skills/repo-arch-engineering/SKILL.md`
- `/Users/saga/code-repos/devweekly.github.io/.trae/skills/repo-arch-engineering/agents/resume.md`
- `/Users/saga/code-repos/devweekly.github.io/.trae/skills/repo-arch-engineering/agents/workspace.md`
- 删除 `/Users/saga/code-repos/devweekly.github.io/.working/unsloth/report-edited.md`

### report.md 质量 QA（18:30）
- 判定：**质量高，满足 Solution Architect 架构报告需求**；结构覆盖并超出 p7.md 成熟度六维（System Context/Style/Data/Strengths/Risks/Insights），另含决策、运行时、Blast Radius。
- 抽查可证伪 claim 全部属实：`__init__.py`=1467 行、`_IS_MLX` 在第 81 行、`_patch_mlx_batch_encoding_to_cuda` 在 149-179、`test_is_mlx_dispatch_gate.py` 第 45 行断言 delegate to `_is_mlx_available()`。
- **待改进 1（主要）**：头部 2x/70%/12x 数字无独立 benchmark 支撑——证据仅证明"内核替换机制存在"（tier A），量级来自 Unsloth 自身 print（ev-008"2x faster"），属厂商自述。建议标注"未独立复测"。
- **待改进 2（次要）**：证据日志 ev-001/ev-022 源行号写 `1430-1433`，实际为 149-179（报告正文写对，insight 层有笔误）；读者交叉核对会困惑。
- **待改进 3（次要）**：ev-xxx 在报告内不展开，独立阅读需开 architecture-insight.json；可加一行来源说明。
