# 2026-08-03

## repo-arch-engineering skill 实跑：ref-only/unsloth
- 用 `.trae/skills/repo-arch-engineering` 全流程分析 `ref-only/unsloth`@`8dffde9`（2265 文件）。
- 产物在 `.working/unsloth/`：artifacts/(profile/directory/module)、evidence-log.jsonl(16条)、repository-model.json、hypotheses.json(6条全 confirmed)、questions/round-1(+reviewed)、architecture-narrative.json、report.md。
- 核心架构结论：架构中心 = import-time monkey-patch HF(transformers/peft/trl) + 手写 Triton/CUDA/MLX 融合内核；单一 import 双后端(_IS_MLX 门，MLX 分支内联 ~1400 行 + CUDA 兼容 shim)；重机械下沉到外部包 unsloth_zoo；Studio 是独立 Tauri 桌面应用(AGPL-3.0) 而核心 Apache-2.0。
- 最大风险：与上游版本强耦合 + unsloth_zoo 版本错配；MLX 后端能力子集(GRPO/DPO 不支持)。
- testing/history 维度本轮未深入（scope 外），记入 report §9 Unknowns。
- 注：之前对话已修过该 skill 的文档矛盾（DESIGN/SKILL/Methodology/model-schema 等），本次为首次实跑。

## repo-arch-engineering skill 加报告字符数 Hard Gate
- 用户要求：最终 report.md 纯内容字符（去空白）必须 >= 15000，否则判定内容/深度不足；用 JS 脚本判定。
- 新增 `.trae/skills/repo-arch-engineering/scripts/check-report-chars.mjs`（exit 0=PASS/1=FAIL/2=用法错；阈值经 MIN_REPORT_CHARS 覆盖，默认 15000）。
- SKILL.md 新增 §6.7 报告字符数硬性门槛（Hard Gate）；Working Folder 结构注脚说明脚本位置。
- quality.md 新增检查项 #11（Hard Gate，优先级最高 FAIL 拒发布）+ suggestion 生成加 `gated-fail` 分支。
- 验证：对 unsloth report(6626 纯内容字符) 跑脚本 → FAIL(exit 1)；MIN_REPORT_CHARS=6000 → PASS(exit 0)。

## repo-arch-engineering：gated-fail 快速根因路由
- 用户要求：6.7 gate 未通过时做快速判断，决定回 Step 3 / 4 / 5 继续。
- SKILL.md 新增 §6.7.1「Gate 未通过时的快速判断与回路」：按优先级 ①覆盖缺口→Step3(Planner 补新问题) ②深度缺口→Step4(对已有问题做 depth pass，不新增问题) ③知识已稳渲染没写足→Step5(确认收敛后 Step6.4 基于现有 model 扩展渲染)。判断依据 repository-model + context.coverage + hypotheses + evidence-log 行数。含防空转规则(同路由 2 次增长<10% → blocked 升级用户)。
- quality.md：#11 补「Gate 未通过路由」表；输出 schema 增加 gate_failed_route + gate_failed_reason 字段及 gated-fail 示例。
- orchestrator.md：mermaid 把 `Quality FAIL→Planner` 改为按 gate_failed_route 分流(step3→PLN/step4→EVI/step5→收敛检查)；调度策略新增「报告深度回炉」路径。

## repo-arch-engineering：sub agents 全量同步 SKILL（任务 15-21 + 历史欠账 5-8 全部清完）
- 修正 gate 脚本目标 report.md→report-draft.md（SKILL §6.7/quality #11/orchestrator 注，Quality 发布前查的是草稿）。
- report.md：渲染深度要求（15000 字符前置约束）+「扩展渲染模式」(step5 路由，expansion_exhausted 兜底)+禁水字数。
- evidence.md：新增「Depth Pass 模式」(step4 路由，不依赖新问题、path:line 级、decision≥2 evidence、密度≥30 行)。
- planner.md：输入补 gate_failed_route/reason；输出 schema 对齐 SKILL Step 3 必填字段；收敛判据改 SKILL §5 五条(knowledge delta)；round_file→round；演化规则 answered→model_updated。
- workspace.md：操作4 发布条件含 Hard Gate(gated-fail 不发布)；summary.json 新状态机(open/investigating/validated/rejected/blocked/model_updated)+by_status 聚合；操作2 补写 questions/round-N.json；phases_completed 命名相位。
- model.md/reasoning.md：字段分区写入同步 schema §2.1（Model=identity/architecture/runtime；Reasoning=design_decisions/evolution/quality_attributes/risks/unknowns/coverage）；model 不再内嵌 hypotheses/evidence/questions，只引用 ID；round_stats questions_answered→questions_closed。
- resume.md 坏链 ../workspace.md→./workspace.md；question-critic.md 注明 reviewed.json 由 Critic 直接写入。
- CHANGE_LOG.md：Step2 双相位、Step4 相位累计规则(Phase2-5 首次完成累计)、新增 Step5 落盘、Step6 补 model_validation+gate 前置；p1.md 加历史存档标注。
- 全局扫尾：旧术语(唯一写入者/连续2轮/round_file/answered_at/invalidated/旧项目名)已清除，gate_failed_route 在 7 个文件交叉引用完整。

## repo-arch-engineering 重跑 unsloth（验证 Hard Gate + §6.7.1 路由实跑）
- 续跑（resume）：working dir 已存在、commit 未变(8dffde9)。旧 report 仅 6626 纯内容字符 → §6.7 gate FAIL。
- 路由判定（§6.7.1）：testing/history total=0（覆盖缺口）→ 路由 Step 3。
- round-2：Planner 生成 8 个问题(q-007..q-014，覆盖 testing/history/deployment深度/MLX能力子集/量化映射/Studio通信/CLI权限/优化器)，Critic 全 approved。
- Evidence 补充 ev-017..ev-030（30 条总）；Model 补全 testing/history/deployment/MLX边界/量化pattern/Studio运行时/CLI模块/优化器 + 新增 dd-6/dd-7、rk-6/rk-7/rk-8；hypotheses 14 条全 confirmed。
- 收敛：14 问题全 model_updated，各维度 ratio=1.0、conf≥0.8。
- 报告重渲染至 15458 纯内容字符 → 过 gate(15000)。新增 §3.3 演进时间线、§4.8 决策耦合、§10 Blast Radius/Change Difficulty（对应 skill 成功标准）。
- 关键新发现：tests/ 292 文件绝大多数覆盖 Studio，库内核数值正确性无 unit test（rk-6）；MLX 是 SFT-only 子集（CUDA no-op 化 + NotImplementedError）；Studio 经 Rust(desktop_backend_owner) 拥有 Python 后端生命周期 + lease_secret/path-grant，localhost uvicorn 通信。
