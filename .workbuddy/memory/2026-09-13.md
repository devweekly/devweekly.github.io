# 2026-09-13

## 1) `temp/agent研究.md`（Enterprise Agent Platform Architecture Review）结论级重构

用户给出 20 条 review，核心是**换结论**，不是改措辞。原报告结论「你们还缺少很多 Agent Platform 基础能力」已被推翻。

### 新结论（报告 §1）

> 技术底座已基本完整（AgentCore + LangSmith + PostgreSQL/pgvector + LiteLLM），
> 下一阶段的主要风险不是能力缺失，而是**多个平台之间的职责边界、运行模型与治理模型**。

### 关键事实更正（review 提供，用来推翻旧结论）

| 旧判断 | 新事实 | 处置 |
| --- | --- | --- |
| Observability / Evaluation 是 P0 缺口 | **LangSmith 已集成** | 移出 P0/P1，改为审查「LangSmith 与 AgentCore 分工」 |
| Hybrid Search 长期应拆成 Retrieval Service | **PostgreSQL + pgvector 已是当前基础设施** | **撤回**该建议，改为「Knowledge / Retrieval 是平台内 Capability」，真正要设计的是 retrieval abstraction |
| Tool / MCP Governance 是最大缺口 | **MCP 走流程 + 架构 Pattern 治理** | 降级；不造重型 MCP Gateway/Registry，只补最小执行元数据 |
| 未涉及 Snowflake | **未来可能接 Cortex Agents** | 新增主线：Design「自建 Runtime 与 Data-Native Runtime 并存」 |

### 结构变化

- 旧：27 节，中文序号 `# 一、`～`# 二十七、`，无标题层级，`我建议你们` 顾问腔，1575 行
- 新：按 review 给的目录重排为 **1 H1 + 15 个 `##` + 64 个 `###`**，2112 行，引用 [1]–[21]（定义数 = 使用数 = 21）
- 新目录：Executive Summary / Current Platform Landscape / Architectural Boundary /
  Agent Lifecycle / Runtime Architecture / Model Architecture / Knowledge & Retrieval /
  Tool & MCP Governance / Identity & Security / Execution & Job Architecture /
  Observability & Evaluation / Data Ownership / Industry Benchmark / Gap Analysis / Target Architecture
- 标题用 review 给的英文原样（正文中文）；章内 `###` 标题中文

### 四处 mermaid（全部 parse 通过）

1. §2.7 平台边界图（review 给的原图）——**Snowflake Cortex Agents 画在 Enterprise Data 下的独立 subgraph「Potential Snowflake Agent Runtime」，不在 AI Platform 下**
2. §3 三平面图（Control Plane / Runtime Plane / Data & Capability Plane）
3. §7.4 Retrieval abstraction（Enterprise Knowledge API → pgvector / Cortex Search）
4. §15.1 Target architecture（Runtime-neutral，多 Runtime 并列）

### 新增的关键设计（旧报告没有）

- **三平面边界**作为 Architecture Review 主框架
- **Runtime abstraction**：`AgentRuntime` 6 个方法（create_run/get_run/cancel_run/resume_run/stream_events/get_result），容纳 AgentCore + Cortex Agents + 未来 Runtime
- **Retrieval abstraction**：`KnowledgeSource` 接口（PostgreSQLVectorSearch / SnowflakeCortexSearch / VendorSearch）
- **双层授权**：平台权限 ≠ 数据平台原生权限（Snowflake privilege 不能被平台授权取代）
- **Runtime identity**（第 9.3 节）：引入第二个 Runtime 后必须显式建模
- **数据职责矩阵**（第 12 章，11 行，Owner + 存储）
- **不要重复造 LangSmith Deployment**（第 10.3 节）：LangSmith Agent Server 已有 Postgres / Task Queue / Runs / Threads / Assistants / Cron，自建理由只应是 Enterprise IAM / AWS / Data / Process 集成
- **P0/P1 重排 + 「与上一版 P0/P1 的差异」对照表**（第 14 章）

### 新增引用

[16] Cortex Search / [17] Cortex Agents / [18] Snowflake 2026-08-28 迁移公告 /
[19] LangSmith Evaluation / [20] LangSmith Agent Server / [21] AgentCore Gateway core concepts。
全部去掉 `utm_source=chatgpt.com`（沿用项目约定：引用不带跟踪参数）。

### 保留下来的内容（review 未否定，仅重新归位）

ZIP Skill 的三类安全风险（supply chain / arbitrary code execution / network egress）、
Skill-Agent-Deployment 三段拆分与 immutable version、Job 状态机与执行模型、
ACL-aware retrieval（权限过滤必须在 retrieval pipeline 内）、document metadata 与数据血缘、
四类 identity、按 Tool Risk 分级的 approval、九类 Registry、
各家厂商分析（AgentCore / LangSmith / Cortex Agents / Foundry / Google / Dify / Letta）、
「从 Agent Framework 进入 Agent Runtime Platform」趋势、
「不要继续横向增加 Agent Framework 能力」这条收尾建议。

### 备份

`/tmp/np3/backup.md`（改前 1575 行）、`/tmp/np3/new.md`（改后）

---

## 2) 第二轮：升级为「金融级 Risk Architecture Review」（15 章 → 20 章）

同一份报告 `temp/agent研究.md` 再做一次结论级升级：从「技术架构评审」变成
「金融服务领域的 Enterprise Agent Platform Risk Architecture Review」。

### 核心判断再次改写

> 技术底座已基本完整。下一阶段风险的核心不是缺少某个 Agent Framework，
> 而是把金融机构既有的模型风险 / ICT 风险 / 数据治理 / 访问控制 / 第三方风险 / 审计要求
> 映射到 Agent 的完整生命周期。

四件事成为安全部分主轴：**Prevent / Detect / Control / Evidence**。

### 结构：15 → 20 章（严格按 review 目录）

新增 6 章：9 AI Risk & Compliance、11 Human Oversight & Approval、14 Third-Party AI Risk、
15 Governance & Three Lines of Defence、16 Regulatory / Control Mapping、20 Architecture Decision Record。
3.4 新增 Policy Enforcement Plane（横向，非第四平面）。
原 Observability 章 11→12、原 Execution & Job 章 10→13（改名 Operational Resilience）、
原 Industry Benchmark 13→17、原 Target Architecture 15→19；原 Data Ownership 章并入 7.6 数据职责矩阵。

### 关键新增内容

- **Use Case Risk Classification L0–L4**（Productivity / Analytical / Decision Support /
  Business Action / Material Decision），控制强度逐级加严 —— 定位为「所有安全策略的第一道闸门」
- **Agent Risk = Model Risk + Execution Risk + Data Access Risk + Action Risk**（Agent Model Risk ≠ LLM Model Risk）
- **Model Registry / Approved Models / Model Validation**（SR 11-7 口径）；区分
  「LiteLLM 解决 Connectivity」与「AI Platform 必须解决 Model Governance」
- **Data Entitlement 从 who → why**（Purpose-based access）；Data Leakage 扩到 8 个位置，
  含 **Trace 本身是敏感数据资产**
- **Tool Risk Classification + Action Policy**（ALLOW / DENY / APPROVE），Registry 可以轻但 Tool Risk Policy 不能轻
- **Kill Switch** 必须 deterministic、不依赖 LLM
- **Audit Evidence ≠ Trace**（12.4）：含 18 个取证字段与 LangSmith Trace vs Audit Evidence 对照表
- **Supply Chain**：Skill 上传升级为 Software Supply Chain Security，运行引用 Skill@version + SHA256
- **六条架构原则**（19.5）：「Agent 可以自主推理，但不能自主突破权限」等

### 做法（可复用）

用「片段文件 + 装配脚本」代替逐条 Edit：
- 新章节写成 `/tmp/np4/f_*.md`
- 原样保留的章（2/4/5）与原章改号（10→13、11→12、13→17、15→19）在脚本里按**行区间整块搬运**，避免重打引入错字
- 脚本内 `rep()` 断言命中数 == 1，任何断言失败即整体不写盘

### 校验结果

| 项 | 结果 |
| --- | --- |
| 行数 | 2112 → **3396**（+1284） |
| 结构 | 1 H1 / 20 章（序号 1–20 连续）/ 92 节 |
| 引用 | 定义 26 = 使用 26（新增 [22] FSB、[23] DORA、[24] SR 11-7、[25] EU AI Act、[26] FSA） |
| mermaid | 8 块全部 parse 通过 |
| 交叉引用 | `第 N 章` / `第 N.M 节` 全部指向存在的标题（脚本校验 OK） |
| 内容不丢 | 逐行比对未命中 65 行，逐条可归因（第 1 章重写、术语按 review 版本替换、Gap 重排、改号） |
| utm | 0 |

### 有意的两处偏差（已在正文说明）

1. review 目录里第 3 章与第 19 章都叫 `Target Architecture` → 正文各加一行中文副题
   （3 = 平台分层；19 = 目标形态）以免同名混淆。
2. review 的 7、8、12、13 章各列 5 个子节，实际保留 6 / 5 / 4 / 5 个，多出的子节用于承接
   原文未被否定的内容（数据血缘、数据职责矩阵、Execution audit、Promotion gate 等），未删内容。

### 备份

`/tmp/np4/backup.md`（改前 2112 行）、`/tmp/np4/prev_repo.md`（cp 前快照）、`/tmp/np4/new.md`（改后）

---

## 3) 第三轮：从「金融风险架构」落到「控制点 + Enforcement Point」（20 章 → 20 章，+1006 行）

同一份报告 `temp/agent研究.md` 再做一次结论级升级：review 的核心诉求是
**「不要再增加一堆独立安全产品，要把原则落成几个确定性的 Enforcement Point，并让它们产生可审计的证据」**。

### 平面模型升级（本轮的骨架变化）

```text
旧：Control / Runtime / Data 三个纵向平面 + 横切 Policy Enforcement Plane
新：Governance / Policy Plane（横切）
    → Control Plane → Runtime Plane → Retrieval PEP / Tool PEP → Enterprise Data
    + Evidence / Audit Plane（独立于 LangSmith）
```

第 3 章小节因此变为 3.1 Governance/Policy Plane、3.2 Control、3.3 Runtime、3.4 Data、
**3.5 Policy Enforcement Points（PEP）**、**3.6 Evidence / Audit Plane**、3.7 对应关系表。
（3.4 → 3.5 的改号导致 5 处交叉引用需要同步，已逐条改。）

### 新增章节

| 位置 | 内容 |
| --- | --- |
| 3.1 | Governance / Policy Plane（Risk / Policy / Entitlement / Approval / Model / Tool / Data / Kill Switch） |
| 3.5 | PEP：统一判定入口、结构化判定请求 JSON、三个前置 PEP、判定链路 mermaid |
| 3.6 | Evidence / Audit Plane，明确与 LangSmith 的分工与「不必新建审计微服务」 |
| 4.6 | Domain Model（Agent/AgentVersion/SkillVersion/Deployment/Run/Policy/PolicyDecision/Approval/Resource/Action/Evidence） |
| 4.7 | Deployment Admission Controller（含 Approval Package JSON） |
| 5.5 | Runtime Guard：`Can this happen?` vs `What should I do?` |
| 12.5 | Agent Evidence Chain（14 节点因果链 + 归属系统表） |
| 19.5 | 只增加五个能力（Policy Enforcement / Entitlement Adapter / Deployment Admission / Evidence Collector / Kill Switch） |
| 19.6 | 六条原则 → **八条 Architecture Invariants**（含中文对照与落点表） |
| 18 | 新增「落地顺序（三阶段）」 |

### 内容改写

- **§7.3** 增 Entitlement Context（Subject × Agent × Purpose × Action × Resource × Classification）与 **Retrieval PEP**
- **§8.3** 增 Action Risk Model（Action READ/WRITE/EXECUTE/COMMUNICATE/TRANSFER/TRANSACTION × Risk LOW/MEDIUM/HIGH/CRITICAL）
- **§8.4** 改名「Action Policy 与 Tool PEP」，补 ALLOW/DENY/APPROVAL 落点与「MCP Governance 走流程、Tool Authorization 必须 Runtime Enforcement」
- **§10.1** 四类身份 → **四层身份链**（User → Agent → Runtime → Tool）+ 禁止共享 service account
- **§10.6** 补「Skill 能执行任意 Python 按 P0 处理」与 Skill type A/B/C 沙箱分级
- **§10.8** 补 Agent Status（ACTIVE/PAUSED/SUSPENDED/RETIRED）进 Control Plane
- **§11.3** 补 Approval 必须是平台能力（approval_id / approver / reason / policy / expires_at）
- **§12.4** 补「Policy Decision 本身必须被记录」+ Evidence Collector + DORA RTS 引用
- **§18** P0 重排为八个控制点：Policy Enforcement / Identity+Entitlement / Retrieval Authorization /
  Tool Action Authorization / Deployment Admission / Audit Evidence / Kill Switch / Skill Supply Chain；
  Use Case Risk Classification 并入 P0-5；Model Risk / Third-party / Data Leakage 降为 P1
- **§19.1** 换成 review 给的金融级目标架构图（Governance & Policy / Control / Runtime / Capability Access / Enterprise Data / Evidence 六个 subgraph）
- 引用 26 → **28**（新增 [27] NIST AI RMF Core、[28] DORA RTS EU 2024/1774）

### 校验

| 项 | 结果 |
| --- | --- |
| 行数 | 3396 → **4402**（+1006） |
| 结构 | 1 H1 / 20 章（1–20 连续）/ 100 H3（90 个编号） |
| 引用 | 定义 28 = 使用 28，无孤儿无未使用 |
| 围栏 | 448（偶）；mermaid 8 块全部 parse 通过 |
| 交叉引用 | 脚本对撞校验：46 处「第 N 章 / 第 N.M 节」全部指向存在的标题 |
| 原文件未命中行 | 109 行，逐条可归因（重写区段：第 1 章结论、第 3 章分层图与 3.4、§7.3、§8.4、§10.1、§12.4、§18 P0/P1、§19.1 图、§19.5 原则） |
| utm | 0 |

### 做法（本次新增的可复用点）

`/tmp/np5/` 与上一轮同构，但把「片段文件」换成 **单文件 `blocks.md` + 分隔符 `@@@@ key @@@@`**，
`build.py` 按 `@@@@ (\w+) @@@@` 切块后用 `BLOCKS[key]` 取用；新增 `prepend` 语义（块要插在锚点行之前）。
两个必须注意的坑：

1. **块体结尾必须 `rstrip("\n")`**，否则块尾空行 + 原文件分隔空行会产生散文区连续空行（本轮修掉 3 处）。
2. **`append` 类的断言要区分 `must_start` / `must_end`**：锚点字符串若取自行尾，必须放 `must_end`，
   否则 `startswith` 断言必然失败（本轮 c33note / c34note 各失败一次）。

### 备份

`/tmp/np5/backup.md`（改前 3396 行）、`/tmp/np5/prev_repo.md`（cp 前快照）、`/tmp/np5/out.md`（改后）、`/tmp/np5/build.py`、`/tmp/np5/validate.py`、`/tmp/np5/blocks.md`

---

## 4) `temp/agent研究checklist.md` 的框架级重构（389 → 542 + 附录 181）

review 的意见是**换掉最高层分类**：不要再自建 14 个 Pillars，直接采用 AWS Well-Architected 六支柱，
把 Agentic AI Lens 与 FSI Industry Lens 作为叠加层注入。这已不是「重排」，而是「整体替换问题集」。

### 新骨架

```text
P01–P06  AWS WAF 六支柱（Agentic AI Lens 的 AGENTOPS01–07 / AGENTSEC01–09 / AGENTREL02–06 作 focus area 注入）
P07–P09  Financial Services Overlay（FSISEC01–16 / FSIOPS / FSIREL / backup）
P10–P14  Enterprise Agent Platform Overlay（Knowledge·Retrieval / Skill 供应链 / Deployment·Evidence / Multi-tenancy / Multi-runtime）
P15      15 条 Architecture Invariants（INV01–INV15，Fail 即阻断）
```

对齐版本：Agentic AI Lens 2026-06-10、FSI Industry Lens 2026-01-27 修订。
原地保留：评审字段表（Status/Evidence/Owner/Risk/Finding/Action/Target/Due）、Evidence 要求、
0–5 分 + `E` 评分、6 个关键证明问题、Architecture Board 评分板（换成新 Pillar 名）。

### 做法：占位符自动编号

`/tmp/np6/f_*.md` 片段里每条问题写 `@Q@`、附录保留项写 `@A@`，`build.py` 顺序编号为
`1..542` / `A01..A181`；叙述里的总数写 `@@TOTAL@@` / `@@ATOTAL@@`，由脚本注入。
**理由**：542 条手写编号必错；而且 review 草稿自身编号就是错的（P03.6 与 P04 交界重复用了一个号，
它自称的「540 detailed checks」与实际条数不符），自动编号顺手修掉。

### 内容不丢失：两层核查

1. 逐字比对：旧版 389 行未命中 275 行 → 太多，不可用。
2. **词元归零法**：只保留「未逐字命中 且 含新文件完全没有的拉丁词元」的旧行 → 275 骤降到 44，
   且都是真候选。据此补回 20 项（`ABAC` / `circuit breaker` / `egress allowlist` / `Multi-AZ` /
   `per-user cost` / `Skill 执行任意 Python` / `Trace masking` 等）。
3. 其余未被新结构覆盖的旧条目全部进「附录 A」，独立编号、不计入主表：
   A.1 平台边界与 Runtime Abstraction、A.2 模型风险与模型注册、A.3 其他补充控制项、
   A.4 Use Case 治理与风险分级、A.5 身份与 Entitlement 细项、A.6 Tool 元数据与 MCP 治理模式、
   A.7 网络安全基线、A.8 容量与发布策略、A.9 第三方与供应链细项、A.10 其他零散保留项。

### 校验

| 项 | 结果 |
| --- | --- |
| 规模 | 1245 → **1729 行**（主表 542 项 + 附录 181 项 = 723） |
| 编号 | 主表 `1..542` 连续唯一；附录 `A01..A181` 连续 |
| 结构 | 1 H1 / 24 H1（15 个 P 小节 + 一/三/四/五/六/七 + 附录 A + 参考）/ 54 H2 |
| 引用 | 定义 19 = 使用 19（review 的 `[5]`/`[6]` 是同一页面，合并后重映射） |
| 围栏 | 54（偶）；无 mermaid（本文档不需要）；无 `utm_source` |
| 遗留交叉引用 | `第 N 章` 全清（旧文里 P02.5 提到「第 12 章」已改指 INV11） |

### 4 处与 review 草稿的差异（已写进正文「七、本版相对上一版的变化」）

1. 问题编号重编为连续编号（修 review 的重复号）
2. 参考条目去重（`[5]`/`[6]` 同页）
3. 旧结构未覆盖条目移入附录 A
4. 上一版的「50 个核心问题」清单作废（编号基于旧结构，无法映射）→ 改用「三、P0 十项红线」

### 备份

`/tmp/np6/backup.md`（改前 1245 行）、`/tmp/np6/prev_repo.md`（cp 前快照）、`/tmp/np6/new.md`（改后）、
`/tmp/np6/build.py`、`/tmp/np6/f_*.md`（10 个片段）

## 第五轮：`temp/agent研究checklist.md` 结构收敛（只留清单，头尾说明入附录）

1729 → 1716 行（`/tmp/np7/`：backup.md / out.md / build.py）。

新骨架：导语（依据 + 规模 + 指向附录）→ `# 一、Checklist 主表（P01–P14，542 项）`（P01 降 H2、
P01.1 降 H3）→ `# 二、Architecture Invariants（15 条）`→ `# 附录 A — 上一版保留项（181 项）`
→ `# 附录 B — 评审框架与说明`（B.1 三层结构 / B.2 记录字段 / B.3 评分方式 / B.4 总览 /
B.5 P0 红线 / B.6 6 个证明问题 / B.7 评分板 / B.8 与 AWS WAF 关系 / B.9 版本差异）→ `# 参考`。

删除的对话句（9 处）：开篇「可以。基于你们现在的实际架构…」、P01.2「上一版明显不够重视」、
P01.4「你们已经决定 MCP…可以保留」+ 架构决策 blockquote、P01.5「你们已经有 LangSmith…」、
P01.6「上一版容易漏掉」、P02.1「上一版把 Memory 混在 Agent State」、P02.7「上一版也明显不足」、
P08.7「上一版应该重点增强」、P13「从上一版的附加项提升」。
改写为中性句 6 处（P01.5 / P02.7 / P13 / P15 导语 / B.5 导语 / B.6 导语 / B.8 两处）。
交叉引用回改 2 处：「一、评审框架」→「附录 B.1」、「三、P0 十项红线」→「附录 B.5」。

校验：主表 1–542 无缺号；附录 A A01–A181 无缺号；`[n]` 使用集 = 定义集（1–19）；
围栏 54（偶）；行级差异 88 条全部归因（标题层级 40 + 有意删除 9 + 有意改写 6 + 重命名）；
词元归零法只剩 4 个词元（Pattern / Process / enforcement / DeepAgents），全部来自被删对话句 → 内容零丢失。

## 第六轮：把「对话稿转成稿」流程沉淀进 `skills/article-de-ai`

`skills/article-de-ai/SKILL.md`：2.0.0 → 2.1.0，943 → 1096 行（+155）。
新增 `# 27. 对话稿转成稿：剥离对话与附录化`（原「最重要的总原则」顺延为 `# 28.`，全篇 1–28 编号连续、围栏 52 偶数）。

27.1 三类非交付内容与处置（对话开场/应答 → 删；起草过程语 → 删；方法·框架·评分说明 → 搬附录）
+ 唯一判据「主语是起草过程或对方原话 → 删；主语是事实/标准/平台 → 留」+ 混血句改中性句。
27.2 目标骨架（包装 H1 收进并列 H1；头尾说明合并为同一附录并平铺 B.1…B.9，不保留旧「一、二、三」章号）。
27.3 脚本搬运三件套 `block(a,b,drops,repls,xform)` + `demote()`，先写 out.md 再 diff 落盘；重型重排指向 skill `longform-md-restructure`。
27.4 回扫两处：旧章号交叉引用 grep、残留对话词 grep（附坏词表）。
27.5 验收判据表（编号连续 / 引用闭合 / 结构完整 / 行级差异全部归因 / 词元归零法 / 无对话残留），附本次实测值。
27.6 交付说明三件事（删了哪些、改了哪些、回改了哪些引用 + 备份与脚本路径）。

## 第七轮：`temp/agent研究.md` 按 review 严格改版（12 问题 + 4 Invariant + FSI 三块）

4402 → 5489 行（`/tmp/np8/`：backup.md（改前） / prev_repo.md / build.py（37 个 op） / f_exec·f_ch3·f_ch5·f_ch10·f_ch13·f_ch18·f_ch19·f_misc.md（8 个片段文件））。

按意见逐条落地：
1. **Policy Plane → Governance & Enforcement Layer**（全局改名 9 处，含 ASCII 方框图按显示宽度重排）；
   三层 policy 拆开归属：AI Platform=Model Governance / Agent Platform=Agent·Action Governance /
   IAM·Data Platform=Enterprise Entitlement / Runtime·PEP=Enforcement（§3.1、§6.1、ADR-22）。
2. **Runtime abstraction 扩写**：AgentExecutionContract + CapabilityContract，补 get_session/get_state/
   checkpoint/interrupt/get_events/get_metrics/get_identity/get_runtime_health/capabilities + supports_*，
   并明确「不要为统一 API 抹平 Runtime 差异」（§5.4）。
3. **新增 §5.6 Runtime Governance Boundary**：Full-control vs Managed Runtime（平台只能做边界控制），
   新增 Invariant 11。
4. **Fail-Closed 语义**（§3.5 + §11.3 + P0-10 + Invariant 10）：按风险分档，HIGH/CRITICAL 一律 DENY。
5. **Evidence 可举证字段**（§12.4）：policy inputs / reason code / authorization source / resource version /
   tool arguments / 各类 hash / approval context + policy decision JSON。
6. **Auditability ≠ Chain-of-Thought**（§12.4 + ADR-29）。
7. **§10.9 Memory Security & Integrity**（P0-9 + Invariant 9）。
8. **§10.10 Goal Alignment / Rogue Agent**：Declared/Allowed/Forbidden + Behavior Baseline + 识别信号。
9. **§11.2 Approval fatigue 与 Agent manipulation**（审批 context 必须由平台生成）。
10. **§10.14 Multi-agent 显式 N/A**（Status/Reason/Trigger/Owner/Review），不再只挂 P2。
11. **§13.6 Agent Cognition 与 Semantic Health**（UP/DEGRADED/BLOCKED）+ Invariant 12。
12. **§10.15 Multi-tenancy 与 Noisy Neighbor**；§16.4 AWS Agentic AI Lens 能力对齐表；§10.13 持续红队。
13. **P0 8 → 10 项**（+P0-9 Memory、+P0-10 Fail-Closed）；P0 = architectural prerequisite，
    引入 Production Gate / exception process / risk acceptance；落地顺序重排（Memory 与 Fail-Closed 前置于 Admission）。
18. **FSI 三块**：§13.7 Backup/Retention/Anti-ransomware、§10.12 SDLC Isolation、§10.11 Privileged Access & SoD。
19. **措辞**：§16.3 三类要求给英文对照（External requirement / Internal control / Architecture recommendation），
    L4 控制标注为「Internal Agent Risk Control Standard」而非监管要求。
20. **§19.2 定位改为 Runtime-aware, Runtime-independent**（Control Plane 不绑定 + Enforcement Plane 按能力落地）。
21. **§19.6 Invariants 8 → 12 条** + 最高层原则「Agent 是不可信的决策参与者，而不是安全边界」（Executive Summary 同步加）。
新增引用 [29]–[35]（AWS Agentic AI Lens agentsec01/04/05、security、reliability、design-principles、FSIREL10），已去 utm_source。

校验：引用 [1]–[35] 使用集 = 定义集；H2 1–20 连续；围栏 574（偶）；mermaid 8/8 parse 通过；
词元归零法「旧有新无」= 0；散文化连续空行 0。**未做整体 30% 压缩**，只对点名的 8 个概念做了
「只定义一次 + 其余引用」的局部去重（净增 1087 行）。

## 第八轮：`temp/agent研究.md` 去掉「版本自指」语气（AI 味）

5489 → 5452 行（`/tmp/np9/`：backup.md / out.md / build.py —— 8 处区间删除 + 56 条整行替换 + 5 条特例）。

用户点名「**这一版收紧的两处**」这类表述 AI 味太冲。全文共清掉 60 处版本自指：
- **删**：加粗变更小标题（`**这一版收紧的两处**`、`**本版新增一条写作规则…**`、`**本版新增的两个 P0 不增加系统数量**`），
  内容并进相邻句子。
- **改为直接陈述**：`本版在这一层新增一个判定环节` → `这一层需要一个判定环节`；`本版把它们彻底拆开` → `这里把它们彻底拆开`；
  `这一版最容易失控的地方` → `这里最容易失控的地方`；`这是本版新增的…` → `这是一条容易被忽略的…`。
- **对照句只留当前结论**：`上一版只写了 ALLOW/DENY/APPROVAL，没有回答这一条。本版明确按风险分档：` → `按风险分档回答：`。
- **主语换成判断本身**（撤回/降级类内容保留）：`上一版报告建议「长期拆成 Retrieval Service」。这个强建议现在撤回。`
  → `「长期拆成独立 Retrieval Service」这个强建议撤回。`
- **跨行结构**：§3 的「三列对照表（上一版|本版）」改成两列「主题|当前定位」；§18 `### 与上一版的差异` → `### 判断调整记录`，
  列名改「原判断|现在的处置|原因」；`### 19.6 从「八条原则」升级为…` → `### 19.6 Architecture Invariants（十二条）`；
  §2.2 的「比上一版报告的：> 引述」结构合并成一句。

**顺带修掉脚本化多轮编辑留下的硬伤**（段落级重复扫描发现）：8 处整段重复
（DORA RTS 段、Trace ≠ Audit Evidence 引用块、Provider abstraction 整块重复等）、
1 处表格行被压成一行（P0-4）、1 处标题前缺空行（§13）。删除重复后词元归零法仍为 0。

校验：版本自指 grep 为空；H1/H2/H3 = 1/20/113；H2 1–20、§10 子节 1–15、§13 子节 1–7；围栏 572（偶）；
引用 [1]–[35] 使用集 = 定义集；mermaid 8/8 parse 通过；重复段落 0；旧有新无词元 0；行级差异 64 条全部可归因。

同时把方法沉淀进 `skills/article-de-ai/SKILL.md`（2.1.0 → 2.2.0，1095 → 1149 行）：新增 `## 27.7 版本自指语气`，
含三档处置表、**「整行替换不要用子串替换」的踩坑记录**（本次真实踩到 `text.replace` 只换片段导致行尾残渣）、
三次收尾扫描命令、以及「段落级重复扫描」这一必做项。

## 第九轮：`temp/agent研究checklist.md` 按 review 补 4 组结构性缺口

1716 → 1974 行（`/tmp/np10/`：backup.md / out.md / build.py）。review 明确「不要再加题，只补结构性缺口」。

新增（59 项，合计主表 601）：
1. **P00 Architecture Decision（用例准入前置层，置于 P01 之前）** ——
   `P00.1 Agent vs Workflow（AD01–AD14）`、`P00.2 Business / User Outcome（BO01–BO10）`；
   写死判据「先证明 deterministic workflow 不可行，才允许用 Agent」，并区分业务 KPI 与平台指标。
2. **P02.0 Threat Modeling / Abuse Case（TM01–TM08）** —— 放在 P02 九组控制项之前；
   TM05 = 「被完全控制时最多能读/改/执行/创建/调用什么、以谁的身份、影响多少租户」。
3. **P01.6 Evaluation Model / Trajectory Evaluation（EV01–EV10）** —— 评 trajectory 而不只是 output；
   区分 deterministic assertion 与 LLM-as-judge；原 P01.6 Recovery 顺延 P01.7（INV13 落点同步改）。
4. **P14 → Runtime / Snowflake / Multi-runtime**，新增 `P14.2 Runtime Isolation（RT01–RT06）`、
   `P14.3 Agent Execution Budget / Runtime Resource Policy（RT07–RT14）`、
   `P14.4 Runtime Lifecycle & Portability（RT15–RT17）` —— 把散在 P04/P05/P03 的各项限额收敛成「执行预算」。

其他：**Invariants 15 → 18**（Inv16 deterministic-first、Inv17 full-compromise 最大影响、Inv18 预算耗尽必须停止）；
新增 **附录 B.10 跨框架映射**（Microsoft → P00、OWASP → P02.0、CNCF → P14.2–14.4 并入；
**NIST AI RMF 只做 Govern/Map/Measure/Manage 交叉引用，不加章节**）；B.1 改「分层结构」并加 P00 层；
B.4 总览 / 三层图 / B.7 评分板 / B.8 执行结构图 / B.9 变化表（第 7 条）全部同步；
参考补 `[20]–[24]`（Azure 架构中心 orchestrations、Microsoft CAF agent 建设流程、OWASP LLM Top 10、
NIST AI RMF、CNCF Platforms White Paper）。

校验：主表 `1–542` 无缺号；AD14 / BO10 / TM8 / EV10 / RT17 全部连续；附录 A `A01–A181` 无缺号；
H1/H2/H3 = 6/35/48；围栏 68（偶）；引用 `[1]–[24]` 使用集 = 定义集；重复段落 0；标题前缺空行 0；
词元归零法「旧有新无」= 0。

**本轮踩到两个会静默丢内容的脚本坑**（已写入 skill `longform-md-restructure` 新增 3-6 节）：
① 双缓冲陷阱 —— `rep_block` 改 `text` 而 `rep` 改 `lines`，最后 `"\n".join(lines)` 把块替换**全部丢掉**
（断言全过、输出少改动），靠 `grep 映射结构 out.md` 为空才发现；
② `rep(key, key)` 这种 no-op 整行替换会把整行截断成半句（B.9 一行表格被截），靠词元归零法发现 `privileged` 消失。

## 第十轮：`temp/agent研究checklist.md` 把 P00 升格为 Architecture Foundation

1974 → 2145 行（`/tmp/np11/`：backup.md / out.md / build.py —— 1 个块 op（P00 全段 28–105）+ 1 个块 op
（Invariants 表 1158–1183）+ 约 40 条整行/片段 rep）。

按 review 落地：
1. **P00 从「Agent 准入判据」升格为通用的 Architecture Foundation（P00-01…P00-20）**，
   分四组：Business Problem & Outcome（01–05）/ Context & Constraints（06–11）/
   Architecture Approach（12–16，含 Buy→Reuse→Extend→Build 四级台阶）/ Decision & Trade-offs（17–20）；
   带 Priority 列（15 个 P0 + 5 个 P1）。方法论来源 ATAM（[25]）与 ADR（[26]）。
2. **P00.5 Architecture Decision Gate**（11 个必答项 + `Fail P0 → 不进入详细架构评审`）；
   **P00.6 Review Artifact**（一页纸流程图，`Build / Buy / Reuse / Do Nothing` 四选）。
   这两节只承载正文、无 ID，不计入主表。
3. **八个评审面**用一张树图把 1..8 映射到 P00.1–P00.4 的 ID 区间；并单列「四个最容易被跳过的问题」
   （Current State / Alternatives 含 Do Nothing / Buy-Build-Reuse / Exit-Reversibility）。
4. **AD01–AD14 与 BO01–BO10 不删也不并列**，降为 `P00.7 / P00.8 Agent 场景展开`，写明
   「不新开一层、评审非 Agent 平台时可跳过」；BO 组注明只补 Agent 特有部分（unacceptable outcome /
   human responsibility / failure 影响 / fallback），避免与 P00.1 重复。
5. **Invariants 18 → 22**：新增 INV19（新建系统前先否决既有系统/流程/配置，→ P00-12）、
   INV20（自建前完成 Buy/Reuse/Extend/Build 分析，→ P00-15）、INV21（重大决策记录 rationale +
   替代方案 + accepted trade-offs，→ P00-17/18）、INV22（有演进/迁移/退出路径，→ P00-20）；
   INV16 落点改为 `P00.3 / P00.7`。加一段说明这六个问题「同时是 P0 检查项与 Invariant」。
6. **计数同步 10 处**：导语规模表、主表 H1、P00 正文自称、附录 A 导入语、Invariants 标题与引言、
   B.4 总览行（P00 = 44）与映射图、B.7 评分板（含 ASCII 列对齐重排）、B.8 执行结构图、B.9 变化表。
   主表 601 → **621**。
7. **B.9 加第 8 条变化**（P00 升格）；**B.10 加两行映射**（ATAM/ADR、AWS Prescriptive Guidance）；
   B.1 改「分层结构」并把 P00 展开八面；B.5 说明 P00 Gate 未通过即不进入 P01–P14；
   参考补 `[25]–[29]`（SEI ATAM、Fowler ADR、AWS Prescriptive Guidance ×3，已去 utm_source）→ 29 条闭合。
8. **措辞自检**：初稿写「P00 不出现 Agent / LLM / MCP 等专有名词」，grep 发现 P00-13 含 `agentic`，
   改为「20 条里只有 P00-13 涉及 Agentic / AI，其余与领域无关」。

校验：主表 1–542 无缺号（542 条）、P00-01…20 连续、AD14/BO10/TM8/EV10/RT17 全连续、附录 A A01–A181 无缺号、
主表合计 621；INV01–INV22 连续；H1/H2/H3 = 6/35/54；围栏 74（偶）；引用 [1]–[29] 使用集 = 定义集；
重复段落 0；标题前缺空行 0；词元归零法「旧有新无」= 0；行级差异 45 条全部归因（P00 段替换 +
Invariants 段替换 + 命名/计数改写）。

方法沉淀进 `~/.workbuddy/skills/longform-md-restructure/SKILL.md`：新增 `### 3-7) 前置层升格 / 层级重定型`
（含「升格层要给双向身份」「Gate/Artifact 类无 ID 内容不计入总数」「原领域组降为展开而非删除」、
「新层六处登记」），并在第 4 节加 `c-3) 计数一致性` 校验片段与「总数出现的 10 个位置」清单。

## 第四轮：`temp/agent研究checklist.md` 结构修正（621 → 594，Coverage 转 Structure）

用户意见明确「不再补更多 checklist，当前主要工作从 Coverage 转向 Structure」，8 条大问题 + 修改优先序。
本轮**只改结构、边界与措辞，不新增检查项**（净 −27 条）。装配目录 `/tmp/np12`（backup/out/build.py + 3 个片段文件）。

十条落地改动：

1. **P00 真正框架中立**：P00-12 去掉「/ Agent」、P00-13 改写为「确定性 vs 非确定性边界」、
   原 P00.5 Gate 里「什么可以交给 Agent / AI」下沉 → 23 条无一条含 Agent / LLM / MCP / autonomy 判断。
2. **新增 `P00.A Agent / AI Architecture Decision`**：AD / BO 不再是 P00 的并列小节，降为 P00.A.1 / P00.A.2。
3. **决策链取代二元判断**：`deterministic? → Workflow / Agent` 改为四级收敛
   （现有方案能否解决 → 是否需要 AI → 最低 AI 档位 ML/LLM/RAG/Agent → 是否需要 autonomy）；
   AD01–AD14 全部改写以对应新链，并写明「Agent 是最后一档，不是默认答案」。
4. **Buy / Reuse / Extend / Build 改成并列 alternatives** + decision matrix 表 + Do Nothing 一列；
   P00-15 措辞从「为什么应该自己建设」改为「为什么在多项 alternatives 中选择当前方案」。
5. **新增 `P00.5 Input / Output Contract（P00-21…23）`**：business input/output → 现实变化/异常/缺失 →
   output 被谁使用与 downstream side effect（decision → action → transaction → external communication 追问链）。
6. **P00 Gate 拆成 Discovery Gate 与 Production Gate**；P0 问题分 Answered / Accepted assumption /
   Validation required 三类状态（原规则与 P00-19 PoC 自相矛盾）。
7. **P07–P09 改写为 FSI Delta**（130 → 100 条，删 30 条纯重复项）：每节写 `Base:` / `Delta:` / `已移出` / `补位`；
   P08.1 Governance 整节并入 P07；章首立「同一 control 只在 base 计分一次」的全局计分规则。
8. **Non-Negotiable 拆两块**：2.1 Runtime / Security Invariants（INV01–18）+ 2.2 Decision Gates（ADG01–04，原 INV19–22）；
   INV08 / INV12 / INV14 加 `[RA]` 并改条件措辞，附「上一版措辞 → 本版措辞 → 为什么改」对照表。
9. **评分方式改多维度**：Maturity 0–5 + Evidence + Risk + Applicability → Result（5 条判定规则）；
   明确 `3 + E` 不再是机械通过条件，只作「已实现且有证据」的下限。
10. **三层结构写进文档**：L1 91 / L2 462 / L3 41（+附录 A 181），每节标题下加层标注（35 处），新增 `附录 B.11 三层结构`
    与 `附录 B.12 编号变更对照`；B.2 记录字段加 Level / Requirement / Applicability；条目元数据引入 `[R]/[RA]/[Rec]`。

**「统计不一致」的真相**：用户指出「顶部 621 与 B.4 的 P01=100 对不上（正文 P01 连续编号只到 90），怀疑 10 项不一致」。
实测 621 是对的 —— P01 = 90 条连续编号 + P01.6 Evaluation Model 的 EV01–EV10 共 100，只是 B.4 没写「构成」导致无法核对。
处理方式不是改数字，而是**让统计可审计**：B.4 增「构成」列 + 新增「编号总账」表
（512 连续 + 23 P00 + 14 AD + 10 BO + 8 TM + 10 EV + 17 RT = 594），并把这段判断写进 B.4 尾注。

编号策略：`1`–`336` 保持不变，`337` 起因 FSI 去重重排，P10–P14 整体 −30（脚本按区间正则重编号，含新旧两向断言）；
版本对照做成 B.12 的逐段表，避免旧引用失效时无从排查。

校验（build.py 内联断言 + 事后扫描）：连续编号 512 无缺号无重复；各 Pillar 计数
P00 47 / P01 100 / P02 142 / P03 65 / P04 20 / P05 19 / P06 8 / P07 19 / P08 53 / P09 28 / P10 20 / P11 19 / P12 13 / P13 12 / P14 29 = 594；
AD14 / BO10 / TM8 / EV10 / RT17、P00-01…23、INV18 + ADG4、附录 A 181 全连续；围栏 42 对（偶）；
引用 `[1]`–`[33]` 使用集 ⊆ 定义集；标题前缺空行 0；行尾空格 0；表格列数（P00 5 列 / INV・ADG 6 列）一致；
重复长段落仅剩三张结构图之间刻意复用的行；**词元归零法**「旧有新无」中文字 0、拉丁词元只剩 p15 / inv20 /
mandatory / score / available / tracking / guardrails / reconstructable. / questions 等已知改名或改写项；
行级差异 167 块 / 删 456 行 / 增 885 行全部归因。

避坑（本轮新增）：
- 统计脚本必须**跳过围栏**：P00.A 决策链里的 `1.` `2.` `3.` `4.` 会被 `^(\d+)\. ` 误计为条目（初版多算 4 条）。
- 区间重编号的断言要分**新旧两向**（我第一版把新号当旧号比，直接踩空）。
- 插入新附录小节时注意 `---` 与 `# 标题` 之间要留空行，否则 `---` 会被解析成标题下划线。
