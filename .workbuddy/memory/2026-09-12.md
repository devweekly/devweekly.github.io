# 2026-09-12

## 整理 post 文字结构：workflow and agent and bpmn.md

- 文件：`src/content/blog/workflow and agent and bpmn.md`（标题「AI时代的业务流程workflow该怎么做？bpmn还是agent」）
- 需求：整理松散的文字结构，**不改语义**
- 注意：该文件**尚未被 git 追踪**（`git ls-files` 无记录），本次整理前无版本可回滚

### 做了哪些结构整理

1. 标题层级统一：正文一级章节 `# 2`–`# 19` → `##`；子节统一 `###`（原 `## ① Intent` → `### ① Intent`、`## Layer 1` → `### Layer 1`）
2. **修正 §8 的层级错位**：原文 ① ② 是 §8 的子节，③ ④ ⑤ 却是独立一级章节（#9/#10/#11）→ 统一为 §8 下的 5 个 `###` 子节
3. 因上一条，后续章节重编号：#12→9、#13→10、#14→11、#15→12、#16→13、#17→14、#18→15、#19→16、#20→17（全文 20 节 → 17 节）
4. 删除 7 处位置不一致的 `---` 分隔线（靠标题分隔）
5. 合并单句引导段，例：「这是非常优秀的模型。因为：」→「这是非常优秀的模型，因为：」；「但它仍然是：> X 而不是：> Y」→ 并为一个引用块
6. 行数 1129 → 1038（-91 行），字符 20427

### 校验（语义未变）

- 4 个 mermaid 块、53 个代码块围栏（106 个 ```）、8 条引用定义全部保留
- 正文引用 [1]×2、[2]×2、[3]–[8]×1，与原文一致
- 关键句抽查 27 条全部命中（含 SAP workflow、compensation、Browser / Computer Use、human accountability 等）

## 第二轮：继续整理 + 正式文章语气 + ASCII→mermaid（同一天）

同一文件。用户追加了两段新内容（第二段 1583 行「四类证据地图」、第三段 1603 行
「Deterministic Business Workflow + Bounded Agentic Execution」），要求继续整理，
并把语气从「AI 对话回答」改成「正式文章」，同时把 ASCII 图换成 mermaid。

### 用户对主题长文语气的新约定（重要）

- 不要第一/第二人称对话口吻：删掉「我查了」「我会把」「我认为」「给你的最终判断」
  「你们平台」「在你补充这个前提之后」这类。改成无人称陈述（「值得注意」「更推荐」
  「可以定义为」）。
- 章节标题不写成口语祈使句（「所以我认为未来会出现…」→「企业 AI Platform 的…」）。
- 「实操」不等于贴代码/清单，要写成工程判断与取舍（沿用 2026-09-08 那条约定）。

### 整理动作（脚本化，逐条断言命中，见 /tmp/tidy_post*.py）

| 环节 | 做法 |
|---|---|
| 标题编号 | 三部分合并为连续 `## 1.`–`## 79.`；中文序号「一、」→ 阿拉伯序号；`#`/`##` 混用清掉 |
| 引用编号 | 三段各自从 `[1]` 起、彼此冲突 → 全文重排为 `[1]`–`[40]`；严格校验「定义数=使用数=40」 |
| 引用位置 | 原有两处「独立成段的 `([X][n])`」（20 处）并入上一句；定义全部移到**文末** |
| 分隔线 | `---` / `-----` 全部删除（只留 frontmatter 两条） |
| 跟踪参数 | 从所有链接剥掉 `utm_source=chatgpt.com`、`_bhlid` |
| 句子碎片 | 10 处「引导句 + 代码块 + 尾巴」合并（如「需要的是：〔块〕而不是漂亮的流程图。」→「需要的是这些，而不是漂亮的流程图：〔块〕」）；§13 的四段职责碎片收成一张列表 |
| 填充句 | 删「这个非常关键。」「这是非常关键的。」「这个变化非常重要。」 |
| 引号 | 正文直引号 `"` 统一为中文弯引号「“ ”」，跳过代码块与引用定义行 |
| ASCII→mermaid | 15 处真拓扑（分支/汇合/循环/树/分层）转 mermaid；线性 `A ↓ B ↓ C` 小片段保留 `text` 块 |
| 坏 mermaid | 修 3 处：2 处把 ASCII 塞进 ```mermaid 围栏（会渲染报错）、1 处围栏写成 ```mermaid id="xxx" |
| frontmatter | tag `blog` → `ai / agent / workflow / bpmn`（按主题长文用主题词的约定） |

### 规模变化

1129 行 → 4035 行（新增两段共约 3200 行）；最终 79 节、33 个 mermaid、169 个 text 块、
40 条引用、59k 字符。

### 可复用方法（下次直接照做）

1. **逐条断言的批量替换脚本**：每个 (old→new) 断言命中次数 == 1，不命中就报错，
   避免静默失配；中途出错可从备份重跑。
2. **中文弯引号归一化匹配**：脚本里写直引号、文件里是「“ ”」时，
   对 text 与 old 同时做 1:1 的引号归一化再定位，长度不变即可精确回写原位置。
3. **文件名含空格要用引号**；行号会因前面替换而漂移，**不要用行号做删除判断**，
   改用「前两个 `---` 是 frontmatter」这类内容判断。
4. **mermaid 语法校验**（build 不会报错，页面才会炸）：
   在 `~/.workbuddy/binaries/node/workspace` 装 `linkedom`，
   用 linkedom 造 window/document 后再 `await mermaid.parse(block)`，
   逐块校验。本次 33 块全部通过。
5. 用户可能在**你干活期间往文件里追加内容**——收尾前务必重新统计行数/标题数，
   发现暴涨就是有新内容进来，需要一并处理。

## 第三轮：按 review 意见做「收敛式重构」（同一天）

用户贴来一份对本文的完整 review（29 条），结论是：**文章里同时存在两套架构**——
前半段 §1–§50 是 Agent-first / Agent-centric Workflow，§51 之后才变成
BPMN-first + Bounded Agent Execution，两套模型没有收敛；而且文件本身就是两轮
内容拼接而成。用户要求「按照意见改，不打折扣」，事实性措辞「逐条改准」。

### 最终骨架（79 节 → 9 部分 / 52 节）

```
引子（核心命题 + 结构说明 + 主线声明）
第一部分 问题定义            §1–4
第二部分 业界趋势（四条路线）  §5–12
第三部分 金融为什么不能照搬     §13–17
第四部分 主线架构             §18–30
第五部分 Agent Task Contract  §31–38
第六部分 一个完整的金融案例     §39–40
第七部分 每一层放谁           §41–45
第八部分 怎么落地             §46–47
第九部分 长期演进             §48–52
```

### 关键手法：把「两套模型冲突」改成「路线对比」

不是删掉前半段，而是**降级**：把 Agent-first 明确标注为「通用 Agent 平台这一路线
的真实主张，是趋势的一部分，但不是本文对金融业务的结论」，然后在第二部分末尾
（§12）用「对 Agent Platform 正确 / 对 Enterprise Business Process 不正确」这张
原有表格做转折，第四部分起进入单一主线。这样既保住了前半段的材料，又让文章不再
自相矛盾。

### 新增的核心章节（review 要求）

- **§20 五层架构**：Process / Runtime / Agent Execution / **Data & Semantic** / Control。
  把 Data 与 Evidence 从 Control Plane 里拆出来（Data 回答「世界是什么样」，
  Control 回答「谁被允许做什么」；Evidence 是执行产物，不是统一存储）
- **§23 责任矩阵**：16 行「问题 → 谁负责」，把「Agent 能不能自主」拆成可逐条确认的问句
- **§26 Business Data Contract**：权威源 / 快照语义 / 版本标识 / 跨系统一致性窗口
- **§27 Audit / Evidence / Agent Trace**：三者回答不同问题、举证对象不同、保留期不同
- **§32 Agent Task Contract**：11 字段完整 YAML + 逐项「缺了会怎样」
- **§34 Analysis Task vs Action Task**：Agent Analysis Task **不应该输出 workflow transition**
- **§25 Workflow 形态的正交分类**：取代原来的「四种形态」（原分类把执行方式/控制方式/
  参与者混在一个维度里，四项不互斥）

### 逐条改准的事实性问题（review 第 2–24 条）

| 原表述 | 改成 |
|---|---|
| 「行业在从 BPMN/Low-code 转向 code-first、runtime-first」 | 行业不是从 BPMN 转向 Agent，而是在把 deterministic 与 agentic 编排**组合**起来 |
| 「Camunda 的方向是把 Agent 塞进 BPMN」 | Camunda 8.7 官方定义是 **deterministic + dynamic 并存**，AI agent 只做非确定性部分；引 [41] |
| 「BPMN 就是漂亮的确定性状态机」 | BPMN 是 **process model**；运行时状态由 Workflow Runtime 持有 |
| 「DMN / Policy 是业务规则和权限合同」 | 拆成四件事：BPMN(流程走向) / DMN(条件判断) / IAM(主体资格) / Agent Policy(能力边界) |
| 「二阶段提交」 | **Governed Action Pipeline**（2PC 有 coordinator/participants/commit-abort，此处不是事务协议） |
| 「Agent 不应该直接碰生产系统」 | Agent 不应该**绕过**身份/权限/工具/审计边界（可以访问，但要走 Tool→Identity→Authorization→Target） |
| 「Dynamic Plan 必须持久化」 | 弱化：持久化的是 Agent Task 执行状态，**不等于企业业务流程的 source of truth** |
| 「80% deterministic + 20% agentic」 | 删除（无依据的数字），改为「可规则化的保持确定性，需语义理解的才引入 Agent」 |
| 「这很可能就是金融服务未来几年的主流架构」 | 当前较容易满足治理/审计/责任要求的一种落地模式（监管材料不足以证明未来主流） |
| 「Agent 负责探索，Workflow 负责固化」 | 标注为**架构推论而非论文结论**（论文只证明 tool sequence 可打包） |
| Palantir Ontology = world model + action model | 标注为「本文的架构解读，不是 Palantir 官方定义」；MCP 表述改为「任何兼容 MCP 的 Agent/Framework」 |
| Snowflake Cortex Agents | 明确「其 orchestration 是 Agent 内部任务编排，≠ BPMN business process orchestration」 |
| Anthropic | 措辞收紧：只关注 Harness/Tool Use/Context/Containment/Eval，**没有**提出企业业务流程架构 |
| Temporal / AgentCore 归在「Execution」 | 拆成 Durable Execution（Temporal/Durable Task）与 Managed Agent Runtime（AgentCore/OpenAI Agents API） |
| 缺 OpenAI Agents API | 补 §8：2026-09-10 发布，托管 Codex harness + 长会话 + 自动压缩上下文 + sandbox + subagents；反证 Agent Runtime ≠ Business Workflow Runtime；引 [42] |

### 被替换/删除的章节（reivew 明确要求）

- 原 §43「Workflow 的四种形态」→ 换成 §25 正交分类（原内容的思想保留在新章节里）
- 原 §48「业界路线归纳为 5 派」→ 换成 §42 能力→产品对照表（+ 三种常见误判）
- 原 §67「企业平台架构收敛为四层」→ 换成 §20 五层架构
- 原 §47 的「80% / 20%」段 → 删除
- 原旧引子（把 Agent-first 当结论的那段）→ 换成新引子

### 本轮的可复用方法（比前两轮更强）

1. **按行区间重组 + PLAN 表**：`build.py` 里维护一张
   `(新标题, [("src",起,止) | ("new", 内容块名)])` 的顺序表，正文原文逐字搬运，
   只有新章节手写。合并多节只需把多个 src 区间放进同一个 block。
2. **new 内容块用 `@@@BLOCK 名字` 分隔放在 blocks*.md**，脚本 split 成 dict；
   内容块自带标题的就把 PLAN 的 head 设成 None，避免重复。
3. **每个 src 区间首尾自动 strip 空行**，这样「标题行+1」作为起点也能安全使用。
4. **内容不丢失核查**（最有价值的一步）：把「有意丢弃的原文行号」列成集合，
   然后逐行检查其余 len>20 的原文行是否出现在输出里 →
   本次 12 处未命中**全部**等于我 rep() 定点修正过的那 12 行，说明零意外丢失。
5. **交叉引用审计**：重排后必须 grep 全文的「第 N 节」「§N」「第 X 部分」，
   逐条核对是否指向正确的新编号（本轮新增 9 处前向引用，全部核对通过）。
6. **层级冲突检查**：插入主题型 `###` 标题时，若原文该处自带
   `### 1. 《论文名》` 这类同级标题，会产生两个同级标题并列 →
   改为不插标题、用引导段，或把原文标题降级。
7. **指代断裂检查**：章节被挪走后，原句里的「这也解释了…」「也就是：」
   「前面的界定是：」会失去指代对象，必须逐处改写。
8. 校验清单：`##` 编号连续、引用定义数 = 使用数、围栏偶数、
   mermaid 逐块 `parse`（37 块 0 失败）、无残留 `utm_source`、`npm run build` 通过。

