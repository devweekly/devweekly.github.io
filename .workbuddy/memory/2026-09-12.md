# 2026-09-12

## 整理 post 文字结构：workflow and agent and bpmn.md

- 文件：`src/content/blog/workflow and agent and bpmn.md`（标题「AI时代的业务流程workflow该怎么做？bpmn还是agent」）
- 需求：整理松散的文字结构，**不改语义**
- 注意：该文件**尚未被 git 追踪**（`git ls-files` 无记录），本次整理前无版本可回滚

### 做了哪些结构整理

1. 标题层级统一：正文一级章节 `# 2`–`# 19` → `##`；子节统一 `###`（原 `## ① Intent` → `### ① Intent`、`## Layer 1` → `### Layer 1`）
2. **修正 §8 的层级错位**：原文 ① ② 是 §8 的子节，③ ④ ⑤ 却是独立一级章节（#9/#10/#11）→ 统一为 §8 下的 5 个 `###` 子节
3. 因上一条，后续章节重编号：#12→9、#13→10、#14→11、#15→12、#16→13、#17→14、#18→15、#19→16、#20→17（全文 20 节 → 17 节）
4. 删除 7 处位置不一致的 `---` 分隔线（靠标题分隔）
5. 合并单句引导段，例：「这是非常优秀的模型。因为：」→「这是非常优秀的模型，因为：」；「但它仍然是：> X 而不是：> Y」→ 并为一个引用块
6. 行数 1129 → 1038（-91 行），字符 20427

### 校验（语义未变）

- 4 个 mermaid 块、53 个代码块围栏（106 个 ```）、8 条引用定义全部保留
- 正文引用 [1]×2、[2]×2、[3]–[8]×1，与原文一致
- 关键句抽查 27 条全部命中（含 SAP workflow、compensation、Browser / Computer Use、human accountability 等）

## 第二轮：继续整理 + 正式文章语气 + ASCII→mermaid（同一天）

同一文件。用户追加了两段新内容（第二段 1583 行「四类证据地图」、第三段 1603 行
「Deterministic Business Workflow + Bounded Agentic Execution」），要求继续整理，
并把语气从「AI 对话回答」改成「正式文章」，同时把 ASCII 图换成 mermaid。

### 用户对主题长文语气的新约定（重要）

- 不要第一/第二人称对话口吻：删掉「我查了」「我会把」「我认为」「给你的最终判断」
  「你们平台」「在你补充这个前提之后」这类。改成无人称陈述（「值得注意」「更推荐」
  「可以定义为」）。
- 章节标题不写成口语祈使句（「所以我认为未来会出现…」→「企业 AI Platform 的…」）。
- 「实操」不等于贴代码/清单，要写成工程判断与取舍（沿用 2026-09-08 那条约定）。

### 整理动作（脚本化，逐条断言命中，见 /tmp/tidy_post*.py）

| 环节 | 做法 |
|---|---|
| 标题编号 | 三部分合并为连续 `## 1.`–`## 79.`；中文序号「一、」→ 阿拉伯序号；`#`/`##` 混用清掉 |
| 引用编号 | 三段各自从 `[1]` 起、彼此冲突 → 全文重排为 `[1]`–`[40]`；严格校验「定义数=使用数=40」 |
| 引用位置 | 原有两处「独立成段的 `([X][n])`」（20 处）并入上一句；定义全部移到**文末** |
| 分隔线 | `---` / `-----` 全部删除（只留 frontmatter 两条） |
| 跟踪参数 | 从所有链接剥掉 `utm_source=chatgpt.com`、`_bhlid` |
| 句子碎片 | 10 处「引导句 + 代码块 + 尾巴」合并（如「需要的是：〔块〕而不是漂亮的流程图。」→「需要的是这些，而不是漂亮的流程图：〔块〕」）；§13 的四段职责碎片收成一张列表 |
| 填充句 | 删「这个非常关键。」「这是非常关键的。」「这个变化非常重要。」 |
| 引号 | 正文直引号 `"` 统一为中文弯引号「“ ”」，跳过代码块与引用定义行 |
| ASCII→mermaid | 15 处真拓扑（分支/汇合/循环/树/分层）转 mermaid；线性 `A ↓ B ↓ C` 小片段保留 `text` 块 |
| 坏 mermaid | 修 3 处：2 处把 ASCII 塞进 ```mermaid 围栏（会渲染报错）、1 处围栏写成 ```mermaid id="xxx" |
| frontmatter | tag `blog` → `ai / agent / workflow / bpmn`（按主题长文用主题词的约定） |

### 规模变化

1129 行 → 4035 行（新增两段共约 3200 行）；最终 79 节、33 个 mermaid、169 个 text 块、
40 条引用、59k 字符。

### 可复用方法（下次直接照做）

1. **逐条断言的批量替换脚本**：每个 (old→new) 断言命中次数 == 1，不命中就报错，
   避免静默失配；中途出错可从备份重跑。
2. **中文弯引号归一化匹配**：脚本里写直引号、文件里是「“ ”」时，
   对 text 与 old 同时做 1:1 的引号归一化再定位，长度不变即可精确回写原位置。
3. **文件名含空格要用引号**；行号会因前面替换而漂移，**不要用行号做删除判断**，
   改用「前两个 `---` 是 frontmatter」这类内容判断。
4. **mermaid 语法校验**（build 不会报错，页面才会炸）：
   在 `~/.workbuddy/binaries/node/workspace` 装 `linkedom`，
   用 linkedom 造 window/document 后再 `await mermaid.parse(block)`，
   逐块校验。本次 33 块全部通过。
5. 用户可能在**你干活期间往文件里追加内容**——收尾前务必重新统计行数/标题数，
   发现暴涨就是有新内容进来，需要一并处理。

