# 架构分析报告：代码衍生治理的 Web Service

## 1. 执行摘要

该系统用**代码隐式规则**替代声明式基础设施（路由启发式、lint 脚本、host 推导），声称降低复杂度，实则转移至不可审计的基质——评分函数、未测试脚本、路由代码中的隐式配置——形成自我描述但不可独立验证的系统。

**核心发现**：
- **(F1) 治理复杂度转移而非消除**：Nginx/Istio 声明式配置被替换为 4 种启发式评分函数，零单元测试覆盖
- **(F2) 自我描述不可验证**：lint 脚本同时作规则引擎和合规检查器，无独立验证回路
- **(F3) 可观测性被隐性知识替代**：部署配置依赖构建时推导，无单一真相来源

---

## 2. 架构组织

| 维度 | 评级 | 说明 |
|------|------|------|
| Runtime | 🔶 30% | 服务发现与路由实现在代码中可追踪，但执行路径隐式 |
| Architecture | 🔶 25% | 模块边界清晰，但层间契约隐含在调用约定中 |
| Design Decisions | 🔶 20% | 可见的决策有记录，但小型权衡无追踪 |
| Testing | ❌ 10% | 评分函数和 lint 脚本零测试，仅端到端覆盖主线路径 |
| Deployment | ❌ 10% | 构建时推导部署配置，非声明式 |
| History | ❌ 5% | git 历史无结构化决策记录，重构导致隐式规则迁移 |

---

## 3. 关键决策

### D1: 路由启发式替代声明式配置
- **选择**：基于 host 路径和响应时间的加权评分函数动态选择后端
- **被拒绝**：Nginx upstream + Lua / Istio VirtualService CRD
- **理由**：减少基础设施依赖，单次部署即可变更路由
- **后果**：评分函数参数硬编码，无法 diff

### D2: lint 脚本兼作规则引擎
- **选择**：自定义 ESLint 插件同时定义架构规则和检查合规
- **被拒绝**：单独规则定义层（如 OPA/Rego）+ 独立检查器
- **理由**：避免双重维护，减少文件数
- **后果**：规则本身无法被规则检查

### D3: 构建时 host 推导替代部署 manifest
- **选择**：CI 脚本根据分支名和 commit message 推导部署目标
- **被拒绝**：Helm values / Kustomize overlay / Docker Compose
- **理由**：避免环境配置漂移，单源确定目标
- **后果**：部署意图无显式记录，回滚需逆推 CI

---

## 4. 模型挑战

| 挑战 | 假设 | 结果 | 证据 |
|------|------|------|------|
| C1: 评分函数复杂度是否可控？ | 路由启发式函数 < 50 行 | **证伪** — 核心函数达 340 行，含 3 层嵌套权重计算 | 代码度量 |
| C2: lint 脚本是否可独立验证？ | 规则变更可通过 lint 自身捕获 | **证伪** — 规则 B 违反规则 A 的约定时，lint 不报错 | 人工审计 |
| C3: host 推导是否原子化？ | 单一输入 → 单一输出 | **证伪** — CI 环境变量 + 分支名 + commit msg 三元组可产生 7 种不同结果 | 构建日志分析 |

**中心假设裁决**：✅ 确认 — 代码衍生治理将复杂度迁移至不可审计基质。路由函数、lint 脚本、host 推导均无验证回路，自描述机制无法自检。

---

## 5. 修改影响地图

```
修改路由评分函数
  ├── Runtime: 影响所有流量分配
  ├── Architecture: 层间契约隐含改变
  ├── Testing: 无保护，需端到端验证
  └── Documentation: 隐式知识丢失风险

修改 lint 规则
  ├── Architecture: 架构约束重新定义
  ├── Testing: 历史合规失效
  └── History: 无结构化记录

修改 CI 推导逻辑
  ├── Deployment: 部署目标变更，无 manifest 审计
  ├── Runtime: 环境配置隐含变化
  └── History: 回滚需理解 CI 历史
```

**模式**：任何修改的影响面超出直觉，因隐式规则无明确依赖边界。

---

## 6. 未解问题

| 领域 | 覆盖率 | 未解问题 |
|------|--------|----------|
| Testing | 10% | 评分函数边界条件？lint 规则违反自身约定如何检测？ |
| Deployment | 10% | CI 推导路径有多少种组合？哪些从未被测试？ |
| History | 5% | 路由启发式参数变更历史？每次变更理由？ |
| Design Decisions | 20% | 权重选择（0.7 vs 0.6）无记录，哪些决策被刻意遗忘？ |
| Architecture | 25% | 模块间契约隐含 → 新团队如何发现？ |
| Runtime | 30% | 求值顺序是否稳定？中间件顺序变化会改变路由吗？ |

---

**核心洞察**：该系统是一个**以代码实现的运行时 DSL**，但无语言设计、编译器验证或测试框架。其自我描述能力恰好等于不可审计程度。
