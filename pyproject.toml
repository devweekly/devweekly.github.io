[project]
name = "devweekly-github-io"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "accelerate>=1.14.0",
    "docling>=2.112.0",
    "httpx[socks]>=0.28.0",
    "networkx",
    "torch>=2.13.0",
    "transformers>=5.8.1",
]
