{
  "structure": [
    "Filesystem-based API routing with service-specific subdirectories (api/<service>/v1|v2)",
    "Module boundaries (src/, server/, api/, tests/, e2e/, scripts/) enforced by lint-boundaries.mjs",
    "API catch-all ([...notfound].ts) with explicit dynamic-route precedence",
    "API contract enforced by sebuf API contract lint script"
  ],
  "behavior": [
    "AI-powered real-time news aggregation, geopolitical monitoring, infrastructure tracking",
    "A2A (Agent-to-Agent) protocol endpoint via api/a2a.ts for inter-agent communication",
    "NLWeb /ask endpoint with token-overlap tool suggestion routing",
    "Host-derived dynamic OAuth and authorization server metadata (RFC 9728, RFC 8414)"
  ],
  "ownership": [
    "Single deployment with self-managed topology and API surface (ownership rule in ARCHITECTURE.md)",
    "Agent-readiness documents derived per-request Host header for multi-origin consistency",
    "Capability inventory (map layers, services, protos, locales) CI-verified from code",
    "API surface contract enforced in CI via enforce-sebuf-api-contract.mjs"
  ],
  "extension": [
    "A2A endpoint for external agent ecosystem integration",
    "NLWeb /ask endpoint for natural language query to tool dispatch",
    "OAuth authorization server metadata endpoint (RFC 8414)",
    "Multi-version API gateways (api/<service>/v1/[rpc].ts, api/v2/shipping/[rpc].ts)"
  ],
  "evolution": [
    "v2 API shipping layer coexisting alongside v1, indicating active API iteration",
    "Agent-readiness elevated to first-class architectural concern (A2A, metadata, tool routing)",
    "Architectural boundary governance via dedicated lint scripts, not convention alone",
    "CI-verified documentation (docs:check, docs:stats) ensuring docs stay synchronized with code"
  ]
}